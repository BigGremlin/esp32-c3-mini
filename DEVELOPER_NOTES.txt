chronos-watch — Developer Notes
================================
Purpose: running log of what was changed, why, and the exact commit(s)/actions involved.
Keep entries in date order, newest at the bottom. Always cite commit hashes where applicable.


2026-07-12 — Why this directory exists, and how the pieces fit together
--------------------------------------------------------------------------
This is a *different* project from the earlier ESP32 watch work (~/OLEDS3Watch-joaquim,
based on joaquimorg/OLEDS3Watch). This one is built on fbiego's "Chronos" ecosystem,
found and already flashed to the board via the Claude app before Claude Code got
involved. Same class of hardware target (Waveshare ESP32-S3-Touch-AMOLED-2.06), but an
unrelated upstream codebase — do not mix the two up, and do not port code between them
without explicitly checking it makes sense for this firmware's architecture.

~/chronos-watch is a `git clone` of a fork (github.com/BigGremlin/esp32-c3-mini) of
https://github.com/fbiego/esp32-c3-mini — remotes: `origin` = the fork (push target),
`upstream` = fbiego's original (pull target for updates, and PR target).

This is THE FIRMWARE — the repo that actually builds and flashes to the watch. It's a
multi-board codebase: one platformio.ini with ~20 [env:...] sections, one per supported
board. Despite the repo being named after fbiego's original ESP32-C3 board, our board
(Waveshare ESP32-S3-Touch-AMOLED-2.06, 410x502 display) is a later-added target:
env:lolin_s3_2_06, board define ESPS3_2_06, extends the shared [esp32] section.
IMPORTANT: platformio.ini's `default_envs` is currently set to `elecrow_c3_1_28`, NOT
our board — any plain `pio run` without `-e lolin_s3_2_06` targets the wrong hardware.
Not yet fixed/changed, just noting it so it isn't a surprise later.

Related directories:
- ~/chronos-esp32 — fork+clone of fbiego/chronos-esp32 (the ChronosESP32 BLE library:
  notifications, weather, time sync, phone-battery relay via setBattery()). Same
  origin/upstream remote pattern as chronos-watch. NOT currently wired into the
  chronos-watch build — platformio.ini's `[esp32]` lib_deps pulls the PUBLISHED
  registry package `fbiego/ChronosESP32@1.9.0`, entirely independent of this local
  clone. If/when we need to patch the library itself, lib_deps will need to be
  repointed at the local fork (e.g. a path/symlink override) — not done yet.
- esp32-lvgl-watchface (bin2lvgl converter) and watch-face-wearfit (.bin dial pool) —
  not yet forked/cloned. Offline tooling for generating new watch faces; output gets
  dropped into chronos-watch/src/faces/ (existing faces there are named by pixel width,
  e.g. 174_410, 1167_410, gated by ENABLE_FACE_* macros in hal/esp32/app_hal.h). Only
  clone these when face-creation work actually starts.
- lewisxhe/XPowersLib — AXP2101 PMU reference library. Not yet forked/cloned; relevant
  only if/when real local battery-percentage hardware reading is added (see below —
  currently this board doesn't seem to have that wired up at all).

Local machine setup done this session: `gh` CLI installed (apt, already in Ubuntu 26.04
universe), authed as GitHub user BigGremlin via `gh auth login` (device-code flow, since
this machine is headless/SSH-accessed — browser auth happens on the connecting laptop,
not locally). User explicitly declined passwordless/NOPASSWD sudo when offered; ran the
one sudo install command themselves in a real terminal each time instead.

PR workflow (not yet exercised end-to-end): branch off `main` in the relevant fork,
commit, `git push -u origin <branch>`, then `gh pr create --repo fbiego/<repo>` to open
against upstream.


2026-07-12 — Battery-percentage: current state is a stub, not a real read
------------------------------------------------------------------------------
Checked hal/esp32/app_hal.cpp for how "battery" is handled on this board target.
Found only two call sites: `watch.setBattery(lvl)` (line ~1367, driven by a debug
slider — `ui_batterySlider` — in a settings/demo screen) and a hardcoded
`watch.setBattery(85)` (line ~2004, looks like a startup default/placeholder). Neither
reads real hardware. No AXP2101 or ADC-based battery code found anywhere in
hal/esp32/ for ESPS3_2_06. So "the battery-percentage fix" isn't a bugfix to an
existing read path — it's wiring up a genuine local battery reading for the first time
on this board. XPowersLib (lewisxhe) is the likely reference for that once it starts.
Not started yet.


2026-07-12 (continued) — Battery-percentage: real AXP2101 read implemented, flashed, CONFIRMED working
------------------------------------------------------------------------------------------------------
Confirmed this board (Waveshare ESP32-S3-Touch-AMOLED-2.06, env:lolin_s3_2_06) is physically
the same PCB as the OLEDS3Watch-joaquim project's board: cross-checked hal/esp32/displays/pins.h
(LCD_CS=12, LCD_SCK=11, LCD_SD0-3=4/5/6/7, LCD_RST=8, TOUCH_RST=9, TOUCH_IRQ=38) against that
project's known-good esp32_s3_touch_amoled_2_06.h BSP header — every pin number matches exactly,
including the AXP2101 PMU's I2C bus (SDA=GPIO15, SCL=GPIO14 — same bus already used for touch in
this codebase, shared, not a separate bus) and address (0x34).

Implementation (all in hal/esp32/app_hal.cpp and platformio.ini, ESPS3_2_06-gated only, no other
board touched):
- platformio.ini, env:lolin_s3_2_06 lib_deps: added `lewisxhe/XPowersLib@0.3.3` (latest tagged
  release at the time; the library's git HEAD is ahead as 0.3.4-dev with a reorganized header
  layout — see gotcha below, don't assume HEAD's API/include-paths apply to the pinned version).
- app_hal.cpp: `#define XPOWERS_CHIP_AXP2101` + `#include <XPowersLib.h>` + `XPowersAXP2101 PMU;`
  global, declared right next to the existing `SensorPCF85063 rtc;` (same file, same pattern,
  same shared I2C bus).
- `PMU.init(Wire, TOUCH_SDA, TOUCH_SCL, AXP2101_SLAVE_ADDRESS)` called in hal_setup(), right after
  the existing `rtc.begin(Wire)` block (reuses the bus `tft.init()` already brought up via the
  touch driver — Wire.begin() with the same pins twice is safe/idempotent on ESP32 Arduino).
  Replaces the hardcoded `watch.setBattery(85)` with a real `PMU.getBatteryPercent()` read,
  passing `PMU.isCharging()` as the second arg (ChronosESP32's `setBattery(level, charging)`).
- hal_loop(): added a 30s-interval re-poll (static `millis()`-gated, ESPS3_2_06-only block) that
  re-reads and calls `watch.setBattery()` again, so the phone-visible battery % actually updates
  over time instead of being fixed at the boot-time value.
- Added a boot-time `Timber.i("AXP2101 battery: %d%%, charging: %d, vbus: %d", ...)` diagnostic
  log (kept permanently, not just for this session's verification — matches the existing
  Timber.e-on-failure convention for rtc.begin() right above it).

Build-tooling gotchas hit and fixed this session (environment setup, not project bugs):
- Ubuntu apt's `esptool` (4.7.0+dfsg) is missing stub-flasher binaries (see nvs-erase entry
  above) — irrelevant for `pio run -t upload` since PlatformIO bundles its own working
  `esptoolpy` package, but matters if ever driving esptool directly again; use `~/.local/bin/esptool`.
- PlatformIO itself wasn't installed; installed via `pip3 install --user --break-system-packages
  platformio` (PEP 668 override, `--user`-scoped only) to `~/.local/bin/pio`.
- First build attempt failed: `python -c ...` in a build_flags backtick — this machine only has
  `python3` on PATH, no bare `python`. Fixed with a user-scoped symlink: `ln -s $(which python3)
  ~/.local/bin/python` (no sudo, `~/.local/bin` already ahead in PATH). This is a machine-level
  gap, would need redoing on a fresh machine/laptop.
- First code attempt used `#include <XPowersAXP2101.hpp>` directly (matches the library's current
  git HEAD layout, which I'd cloned to a scratch dir to check the API) — but the pinned `0.3.3`
  release still uses the old layout: `.tpp` files behind an umbrella `XPowersLib.h`, selected via
  a `#define XPOWERS_CHIP_AXP2101` before including it. Fixed as described above. **Lesson: when
  checking a dependency's API by cloning its repo, check out the actual pinned tag, not just
  default-branch HEAD — they can differ.**
- `src/common/generated_features.h` gets rewritten by the project's own pre-build script
  (`support/header_gen.py`) on every `pio run`, and its include order isn't stable between runs
  (looks like unsorted directory-listing order) — showed up as unrelated-looking diff noise after
  building. Reverted with `git checkout -- src/common/generated_features.h` before considering the
  working tree clean; do this after every local build, it's not a real change.

Verified end-to-end: built clean (`pio run -e lolin_s3_2_06`), flashed via `pio run -e
lolin_s3_2_06 -t upload --upload-port /dev/serial/by-id/usb-Espressif_USB_JTAG_serial_debug_unit_80:B5:4E:F3:30:F4-if00`,
captured serial boot log, confirmed real reading: `AXP2101 battery: 100%, charging: 0, vbus: 1` —
correct given the watch was on USB power with a full battery (not actively charging since already
full, matches expected AXP2101 behaviour). Committed 2026-07-13 as `00a1022` on branch
`fix/axp2101-battery-read` (see the 2026-07-13 branch-strategy entry below for why it isn't on
`main`).

Not yet done: BLE-paired verification (phone app showing the real %, not just the serial log);
this was confirmed via serial diagnostic only. Also not yet tested: behaviour on battery power
only (no VBUS), or mid-charge (0 < pct < 100, charging=1) — only the "full + plugged in" state was
observed this session.


2026-07-12 (continued) — Real battery now wired into watch faces + Settings display, flashed
-------------------------------------------------------------------------------------------------
User feedback after the first battery fix: two of the enabled watch faces show a battery
indicator (`174_410`/`228_410`/`1167_410`/`2051_410`/`1041_410`/`2151_410` are the six faces
enabled for this board via ENABLE_FACE_*_410 in hal/esp32/app_hal.h; confirmed by viewing each
`watchface.png` preview that `1167_410` has a clear "battery %" bar/icon at top, and `2151_410`
— the one with the large orange moon the user likes — also has a battery-driven element), and
neither was showing anything real. Also: the "Battery" slider in Settings (`ui_batterySlider`,
`ui_batteryPanel`/`ui_batteryLabel`) is user-draggable, which the user correctly flagged as wrong
now that real battery hardware exists.

Root cause for the faces: found a single shared code path, `update_faces()` in app_hal.cpp,
computes one `battery` int for ALL watch faces via `watch.getPhoneBattery()` — i.e. every face's
battery indicator was actually showing the *connected phone's* battery (relayed over BLE), never
the watch's own. Confirmed via ChronosESP32 library header (`~/chronos-esp32/src/ChronosESP32.h`)
that `_batteryLevel` (the watch's own, set via `setBattery()`) has NO public getter — only
`_phoneBatteryLevel`/`getPhoneBattery()` is exposed — so faces structurally could not have shown
the real value even if someone tried, the API doesn't support reading it back.

Fix (all ESPS3_2_06-gated, no other board affected):
- Added a global `int watchBatteryPercent = 100;` next to the `PMU` object — our own tracked
  copy of the last good AXP2101 reading, updated by both the boot-time read and the 30s
  `hal_loop()` poll (added in the previous entry).
- `update_faces()`: `#if ESPS3_2_06` now sources `battery` from `watchBatteryPercent` instead of
  `watch.getPhoneBattery()`; `#else` unchanged for every other board (still phone battery, as
  originally designed there).
- Settings slider: `lv_obj_remove_flag(ui_batterySlider, LV_OBJ_FLAG_CLICKABLE)` right after
  `ui_init()` in hal_setup() (ESPS3_2_06-only) — stops it being draggable at all, without touching
  the auto-generated `src/ui/ui.c`. Both the boot-time read and the 30s poll now also call
  `lv_slider_set_value(ui_batterySlider, watchBatteryPercent, LV_ANIM_OFF)` and
  `lv_label_set_text_fmt(ui_batteryLabel, "Battery %d%%", watchBatteryPercent)` directly — bypasses
  needing to fire a synthetic LVGL VALUE_CHANGED event to get the "Battery NN%" label text to
  update (that logic normally only runs from `ui_event_batterySlider`'s VALUE_CHANGED handler,
  which itself only fires on user drag or an explicit `lv_event_send()`).
- `onBatteryChange()`/`ui_event_batterySlider` (the old drag-to-fake-a-level handler) intentionally
  left in place, untouched — now simply unreachable by touch since the slider isn't clickable, no
  need to delete it (and it's still the correct/only behaviour on every other board, which don't
  have a real PMU and still rely on this control to simulate a level for `getPhoneBattery()`-driven
  face testing).

Built clean, flashed, reconfirmed boot log still shows `AXP2101 battery: 100%, charging: 0,
vbus: 1`. **Not yet visually confirmed on-device** that the moon face (2151_410), 1167_410, and
the Settings screen actually render 100% correctly and that the slider truly can't be dragged —
user is doing their own significant testing pass before any of this gets committed. (Update
2026-07-13: 1167_410 specifically WAS checked, and did not render correctly — see below. The
other four enabled faces are still unconfirmed.) Included in the same `00a1022` commit as the
previous entry.

**Open question for later, not addressed:** the four other enabled faces (`174_410`, `228_410`,
`2051_410`, `1041_410`) were not individually checked for whether they also render a battery
element — the `update_faces()` fix applies uniformly to all of them since it's one shared
function, so they should also now show the real value if they bind to it at all, but their
`watchface.png` previews weren't inspected for a battery indicator specifically (only 1167_410
and 2151_410 were, per the user's report of "along with one of the other watch faces").


2026-07-12 — "Funny state" after reboot: screen goes dark, unresponsive to touch — RESOLVED, nvs erased
---------------------------------------------------------------------------------------------------------
Symptom: watch showed a boot splash, then went dark and stayed dark; appeared totally
unresponsive to touch. A soft reset (DTR/RTS pulse over the native USB-Serial-JTAG
link, same mechanism esptool/idf-monitor use — /dev/serial/by-id/usb-Espressif_USB_JTAG_serial_debug_unit_80:B5:4E:F3:30:F4-if00)
produced a clean boot log (`Setup done`, `v6.3 [fbiego]`, ESP32-S3, MAC 80:b5:4e:f3:30:f5)
but the dark-screen/no-touch-response symptom persisted. A full physical battery
disconnect (~10 min, the known fix for this hardware family's occasional stuck-PMIC
state — see the OLEDS3Watch project's equivalent note) made no difference either.

Root cause, found by reading source: brightness is persisted via the ESP32
`Preferences` library (`prefs.begin("my-app")`, app_hal.cpp:1837) into the `nvs`
partition (partitions_16M.csv: offset 0x9000, size 0x5000 / 20KB) — separate from the
firmware itself (`app0`) and watch-face storage (`ffat`). At boot it loads
`prefs.getInt("brightness", 100)`. Critically, the main loop's screen-wake path
(app_hal.cpp ~2330) re-applies that *same stored value* every tick whenever the screen
is "on" (`screenBrightness(lv_slider_get_value(ui_brightnessSlider))`), and the touch
handler (`my_touchpad_read`, app_hal.cpp:271-284) does correctly call `screen_on()` on
a real touch — so the "wake" logic was working, but brightness was being force-set back
to whatever was last saved (near 0) every time. Matches the user's own account: the
last action before the funny state was dragging the brightness slider down.

Fix applied: erased just the 20KB `nvs` partition via `esptool erase-region 0x9000
0x5000` — resets brightness/timeout/touch-calibration/etc. back to firmware defaults,
does not touch firmware (`app0`) or watch-face data (`ffat`). Confirmed working
on-device afterward (screen now shows content on boot, user confirmed).

Tooling note for next time: the Debian/Ubuntu apt package `esptool` (4.7.0+dfsg) is
missing its bundled stub-flasher binaries (dfsg repackaging strips precompiled blobs),
so `erase_region`/most flash operations fail even with `--no-stub` (ROM bootloader
alone doesn't support erase_region). Fix: installed real esptool from PyPI into
`~/.local` via `pip3 install --user --break-system-packages --ignore-installed
--force-reinstall esptool` (got 5.3.1). Use `~/.local/bin/esptool` (note: no `.py`
suffix needed/preferred; `esptool.py` invocation form is deprecated) for any future
flash-level operations on this board, not the apt one.

Possible side effect not yet confirmed either way: erasing `nvs` may also have cleared
BLE pairing/bonding state (same partition, no dedicated bonding partition in
partitions_16M.csv) — may need to re-pair with the Chronos phone app. Not confirmed
broken or working, just flagging it as untested.


2026-07-13 — 1167_410 face: battery bar wraps to empty at full charge, percentage missing its ones digit — FIXED, flashed, confirmed
------------------------------------------------------------------------------------------------------------------------------------
User-reported symptom, on the real device with the now-real AXP2101 reading in place (previous
entry): the 1167_410 face's battery bar icon looked fine while charging but dropped to empty right
at 100%, and the percentage number was missing its last digit (40% displayed as "4").

Root cause, in `update_status_1167_410()` (src/faces/1167_410/1167_410.c):
- Bar: `face_1167_410_2_112000` (11-frame icon) was indexed with
  `(battery / (100 / 11)) % 11`. Integer division: `100/11` truncates to `9`. At `battery=100`,
  `100/9=11`, `11%11=0` — wraps back to the empty frame exactly at full charge.
- Digits: three image slots existed (`face_1167_410_3_171192` at x=189 showing `battery/10`,
  `face_1167_410_4_171192` at x=172 showing `battery/100` correctly hidden except at exactly 100%,
  `face_1167_410_5_171192` at x=155 showing `battery/1000` — always zero for a 0-100 value, and
  *not* hidden). No slot ever computed `battery%1` (the true ones digit), so the tens digit at
  x=189 was the only thing that ever visibly changed — hence "4" instead of "40".

Fix (src/faces/1167_410/1167_410.c + .h, this face only — bug pattern likely exists in sibling
faces generated by the same bin2lvgl pipeline, e.g. plain `1167`, but only 1167_410 was touched
since it's the only one of that family enabled/tested on this board):
- Bar index changed to `(battery / 10) % 11` — maps 0-100 onto 0-10 directly, no wraparound
  possible for any value in range.
- Added a new digit image object `face_1167_410_47_171192` (positioned x=206, 17px right of the
  tens digit at x=189 — matches the existing 17px digit pitch), driven by `(battery / 1) % 10`.
- Hid the always-zero thousands-digit slot (`face_1167_410_5_171192`) permanently at init — it's
  meaningless for a 0-100 value and was left visibly showing a stray "0".
- First flash attempt showed the new ones-digit overlapping the '%' glyph badly (glyph is 15px
  wide, new digit slot only had ~4px clearance before the '%' at x=210). Moved the '%'
  (`face_1167_410_6_175276`) from x=210 to x=227 to clear it.

Verified: `pio run -e lolin_s3_2_06` builds clean, flashed via
`pio run -e lolin_s3_2_06 -t upload --upload-port /dev/ttyACM0`, user confirmed on-device the bar
holds full at 100% and the percentage now reads correctly as two digits with the '%' clear of the
new digit.

Committed as `e7ac3b1` on branch `fix/1167-410-battery-digit` (see branch-strategy entry below).


2026-07-13 — Investigated: touch-to-wake battery drain, and AXP2101/RTC power-off behaviour (no code change, findings only)
------------------------------------------------------------------------------------------------------------------------------
User observation: "touch to wake" seems to drain the battery heavily, and when the battery goes
fully flat the watch appears to power off but the RTC keeps time regardless — asked how the
firmware decides to power off, and whether it's safe for the RTC to be drawing off a flat battery.

Findings, from reading hal/esp32/app_hal.cpp (no fix attempted, this was pure investigation):
- There is no real sleep state anywhere in this firmware — grepped for `esp_sleep`,
  `esp_light_sleep_start`, `esp_deep_sleep`, `gpio_wakeup` across `src/` and `hal/`: zero hits.
  "Screen off" (the `screenTimer` timeout, ~line 2387, "Screen timeout" log) only calls
  `screenBrightness(0)` — for this board that's `tft.setBrightness(0)`, a backlight PWM call. It
  does not sleep the display panel or the ESP32.
- `hal_loop()` (line ~2201) runs unconditionally every ~5ms regardless of screen state:
  `lv_timer_handler()` (full LVGL redraw/animation tick — the seconds hand keeps animating with
  the backlight off), `watch.loop()` (ChronosESP32 BLE + touch-input processing), plus a 30s
  AXP2101 battery poll over I2C (added in the 2026-07-12 battery-reading work above). So the CPU,
  touch controller, LVGL, and BLE stack are at full active-mode current draw the entire time the
  screen is dark — "touch to wake" is not waking from sleep, there's no sleep to wake from. This is
  the actual drain source, not the touch polling itself.
- Grepped every `PMU.` call site in the codebase: only `.init()`, `.getBatteryPercent()`,
  `.isCharging()`, `.isVbusIn()` exist. No `setSysPowerDownVoltage`, no low-battery shutdown IRQ
  config, nothing — the firmware never tells the AXP2101 when to cut power. The power-off observed
  at empty battery is the AXP2101's own factory-default hardware over-discharge protection, not a
  firmware decision.
- RTC-survives-flat-battery: not verifiable from this repo (board wiring/schematic, not firmware),
  but the PCF85063 is a separate low-power timekeeping chip and boards built around it near-
  universally give it its own small backup source (backup cap or coin cell) isolated from the main
  battery rail, specifically so it survives the PMIC cutting the main rail — standard, expected,
  safe design. Would need the Waveshare board schematic to confirm definitively for this specific
  unit; not available in this repo.

Not started: an actual real-sleep implementation (light sleep + touch-controller IRQ as wake
source) would fix the drain, but is a real feature addition, not attempted this session.


2026-07-13 — Git identity + branch strategy set up for upstream contribution
------------------------------------------------------------------------------
Git had no user.name/user.email configured on this machine at all — first commit attempt failed
outright. Set repo-local only (not --global, deliberately — this machine may host other repos
under a different identity later): `user.name = BigGremlin`, `user.email =
72744346+BigGremlin@users.noreply.github.com` (GitHub-provided noreply address for the BigGremlin
account, id 72744346 via `gh api user`, chosen over the personal email to keep it out of public PR
history).

Branch strategy agreed with the user, since `main` had two unrelated uncommitted changesets sitting
directly on it with no branch cut (the AXP2101 battery-read work from 2026-07-12, and the
1167_410 digit fix from today) and both are headed for upstream PRs against `fbiego/esp32-c3-mini`:
- `main` stays a clean mirror of `upstream/main` — fast-forward pulls only, never commit here
  directly. It's just the base new branches get cut from.
- One topic branch per fix intended for upstream, e.g. `fix/axp2101-battery-read`,
  `fix/1167-410-battery-digit` — small and focused, one bug per PR. Push to `origin`
  (BigGremlin/esp32-c3-mini), `gh pr create --repo fbiego/esp32-c3-mini` against upstream.
- `gremlinDev`: long-lived branch off `main`, merges in every fix branch (so it always has
  everything for actually building/flashing day-to-day) plus anything not intended for upstream at
  all (personal tweaks, board-specific hacks too narrow for the original project, this notes file
  itself). Periodically merge/rebase in `main` (after pulling `upstream`) to stay current.

Since both existing changesets are clean, general bug fixes (not personal-only hacks), both got
their own topic branch off `main` and were committed there (`fix/axp2101-battery-read` = `00a1022`,
`fix/1167-410-battery-digit` = `e7ac3b1`), then merged into a new `gremlinDev` branch
(merge commit `3c9ecfc`) alongside this notes file. `main` itself remains untouched at `888f308`
(no local commits on it).

Update same day: both branches pushed to `origin` and PR'd against `fbiego/esp32-c3-mini` —
https://github.com/fbiego/esp32-c3-mini/pull/45 (AXP2101 battery) and
https://github.com/fbiego/esp32-c3-mini/pull/46 (1167_410 digit fix). Pushing needed
`git -c credential.helper='!gh auth git-credential' push ...` since plain `git push` has no
credential source configured and `gh auth setup-git` would have written to git config (avoided
per standing instruction not to touch git config beyond what's explicitly asked for).


2026-07-13 (continued) — New experimental analog face for this board: ported "75_2" (Analog, 240x240) to 410x410 — WORKING, gremlinDev only, not upstream material
-------------------------------------------------------------------------------------------------------------------------------------------------------------------
User wanted an analog watchface (rotating hands, not a digit-image digital face like all six
faces already enabled for ESPS3_2_06 — see the 1167_410 entry above). None of the five real faces
on this board are analog (confirmed by grepping every `update_status_*_410` for
`lv_image_set_rotation` — zero hits across all five). The repo does have real analog designs (e.g.
`75_2`, registers as "Analog", genuine hour/minute/second hand rotation), but only pre-converted
for other resolutions (240x240, round 466x466) — nothing at 410-wide for this screen.

Tooling set up (none of this existed on the machine before):
- Cloned `fbiego/esp32-lvgl-watchface` (the actual bin2lvgl converter, `src/faces/bin2lvgl.jar` —
  prebuilt, no need for `kotlinc`) and `fbiego/watch-face-wearfit` (the `.bin` dial pool + a
  `resizer.pyc`/`imager.jar`/`watchface.jar` toolset) to `~/esp32-lvgl-watchface` and
  `~/watch-face-wearfit` (plain clones, not forks — these are tools we consume, not projects we're
  contributing to).
- `bin2lvgl.jar` needs a JRE; none was installed. Got one with zero sudo via the `install-jdk` pip
  package: `pip3 install --user --break-system-packages install-jdk` then
  `python3 -c "import jdk; jdk.install('17')"` → installed to `~/.jdk/jdk-17.0.19+10`.
- `watch-face-wearfit/tools/resizer.pyc` needs Python 3.11 specifically (bytecode magic-number
  mismatch against the system's 3.14) and Pillow. Got Python 3.11 with zero sudo via `uv`
  (installed via `pip3 install --user --break-system-packages uv`, then
  `uv python install 3.11`), then `uv venv --python 3.11 .venv` + `uv pip install Pillow` inside
  `watch-face-wearfit/tools/` for a self-contained interpreter with Pillow available.
- **`resizer.pyc` turned out to be a dead end**: despite the README documenting
  `py resizer.pyc watchface.bin 240 360` (from-size, to-size args) and the tool printing a
  "AxA >> BxB" banner reflecting whatever's typed, the actual resize target is hardcoded — same
  output file (byte-identical size) regardless of what width arguments were passed. Confirmed via
  `strings resizer.pyc`: the banner text is a literal constant, not built from argv. Do not trust
  this tool's documented interface; if a real fix is ever wanted, decompiling the bytecode (no
  `.py` source ships in the repo) would be the only path, or just keep using the approach below.

Actual approach used (bypasses resizer.pyc and the proprietary `.bin` dial format entirely):
run `bin2lvgl.jar` once on the 240x240 source `.bin` to confirm/inspect structure, but the real
source of truth was chronos-watch's own already-converted `src/faces/75_2/assets/*.c` — decode
those LVGL C image arrays directly back to pixel data, scale with Pillow, re-encode to the same
LVGL C format. New helper module: `tools/facegen/lvgl_img.py` (decode/encode/format functions),
driver script `tools/facegen/gen_75_2_410.py` (scales 75_2 → 75_2_410 specifically; adjust the
`SRC_DIR`/`OLD`/`NEW`/target-width constants at the top to reuse for a different source face or
target resolution).

**Two real format gotchas hit and fixed, worth knowing before touching these asset files again:**
1. `.header.always_zero = 0,` vs `.header.magic = LV_IMAGE_HEADER_MAGIC,` — bin2lvgl emits
   different header-field names across files/versions (even inconsistently within the same
   `75_2` face — the alpha-less background asset used `magic`, the alpha-bearing hand/text assets
   used `always_zero`). This project's pinned LVGL (9.3.0) only has `magic` on
   `lv_image_header_t` — `always_zero` doesn't exist and fails to compile. This never surfaced
   before because `75_2` was never enabled on any board that actually got built (its
   `ENABLE_FACE_75_2` guard was never defined for `ESPS3_2_06`) — first time enabling it here is
   what exposed the latent bug. Fix: always emit `.header.magic = LV_IMAGE_HEADER_MAGIC,`.
2. **Pixel data layout is planar, not interleaved.** For `LV_COLOR_FORMAT_NATIVE_WITH_ALPHA`
   (RGB565 + alpha), the byte array is NOT `[R,G,alpha, R,G,alpha, ...]` per pixel — it's all
   `w*h*2` RGB565 color bytes first, THEN all `w*h` alpha bytes after (confirmed by exact byte
   count: a 10x66 image is 1980 bytes = 660*2 (color) + 660*1 (alpha), not divisible cleanly any
   other sensible way). Decoding as interleaved-per-pixel produces a subtly-broken image where
   most fully-opaque regions look fine (background, solid fills) but anything with real alpha
   transitions — edges, thin strokes, text — comes out as scattered colour speckling, because
   each "pixel" read is actually built from bytes belonging to 1-2 *different* real pixels plus a
   stray alpha byte. This is exactly what the user saw on the first flash (hands + weekday text
   speckled, solid background fine) and initially misdiagnosed as a resize-filter ringing problem
   (tried LANCZOS→BICUBIC, tried adding alpha premultiplication — neither helped, because neither
   was the actual bug). Confirmed by direct pixel inspection of a decoded-but-unscaled hand image
   at 8x nearest-neighbour zoom — the speckling was present before any resize step ran at all,
   which is what finally pointed at the decode step itself rather than the resize.

Face itself: root object's black background is used for letterboxing — since 75_2 is a 240x240
square design and this screen is 410x494 (not square), the scaled 410x410 result is vertically
centered with a 42px black margin top and bottom (`Y_OFFSET = (494 - 410) / 2` in the script)
rather than distorting it into an oval to fill the full height. All positions and hand pivot
points scaled by the same 410/240 factor as the images themselves, so rotation stays correctly
centered.

Wired up ESPS3_2_06-only: `ENABLE_FACE_75_2_410` added to `hal/esp32/app_hal.h`'s `ESPS3_2_06`
block, `init_face_75_2_410`/`update_check_75_2_410` added to `ui_watchfaces_init()`/
`ui_update_watchfaces()` in `src/ui/ui.c`. New face lives at `src/faces/75_2_410/`. Builds clean,
flashed, user confirmed hand rotation, letterboxing, and (after the planar-decode fix) the
speckling are all good.

**Deliberately NOT going to `fix/*` / upstream** — this is this-repo-specific tooling output
(custom Python scripts, not bin2lvgl's own documented workflow, since that workflow's resize step
turned out broken) and a hand-picked design decision (letterbox vs stretch) rather than a general
bug fix. Belongs on `gremlinDev` only. Not yet committed as of writing this entry — see git log
for whether that's since changed.


2026-07-16 — Chasing a "Casio" watchface seen in the Chronos App; app-side install confirmed a no-op
------------------------------------------------------------------------------------------------------
User spotted a Casio-style face under the Chronos App's "ESP32 Files" section and asked to get it
converted and flashed in. Two things got confirmed along the way, worth keeping straight:

**In-app "Install" does nothing on this firmware.** User tried tapping Install on the face
directly from the phone; it retried a few times and failed. Checked whether that leaves any
partial/corrupt file behind that would need cleanup — it doesn't, because there's nothing to
clean up. `~/chronos-esp32/src/ChronosESP32.cpp`'s `onWrite()` (~line 1358) has:
```
if (pData[0] == 0xB0) { // bin watchface chunk info }
if (pData[0] == 0xAF)  { // bin watchface chunk data }
```
— both bodies are empty, comment-only stubs. The BLE packets for a watchface push arrive and are
silently dropped; nothing is written to flash/LittleFS, no ack goes back to the app (which is why
it just keeps retrying then gives up). Matches the README's own admission that "Installable
custom watchfaces from Chronos app" is marked ToDo/WIP for LVGL 9. So: app-side install is a dead
end on this codebase as it stands — the only working path for a new face is still the
precompiled-into-firmware route documented above (75_2_410 etc.), which means sourcing the raw
dial `.bin` some other way (candidate: `fbiego/watch-face-wearfit`, ~500+ dials across board-model
folders, but they're keyed by anonymous numeric ID with no style/brand metadata — no "casio"
string anywhere in that repo — so matching one to the specific face the user saw requires a visual
compare against a screenshot, not a text search).

**Resolution re-confirmed: 410x494 is the number that matters for face-shopping, not 410x502.**
Got momentarily mis-stated mid-session as "the active board is elecrow_c3_1_28, 240x240" by trusting
`platformio.ini`'s uncommented `default_envs` line instead of checking real build evidence — this
exact trap was already flagged in the 2026-07-12 entry above ("default_envs is currently
elecrow_c3_1_28, NOT our board") and got re-tripped over anyway. Corrected by checking
`.pio/build/` directly, which only contains `lolin_s3_2_06/` — that's the real target. For
resolution specifically: the BSP's raw panel buffer (`hal/esp32/displays/pins.h`,
`SCREEN_WIDTH`/`SCREEN_HEIGHT` under `ESPS3_2_06`) is 410x502, but the number the firmware
actually advertises to the Chronos App — `CS_410x494_200_RTF` in
`chronos-esp32/src/ChronosESP32.h:260`, "410x494, 2.0 inches, Rectangular" — is 410x494, 8px
shorter. That 494 figure is almost certainly what the app used to filter/offer the Casio face in
"ESP32 Files" in the first place, and matches the already-documented 75_2_410 letterboxing math
above (`Y_OFFSET = (494 - 410) / 2`). **Takeaway: when hunting for a compatible dial, 410x494 is
the figure to match against — 502 is only the raw hardware buffer, not the usable/advertised
watchface canvas.**

Status as of writing: still waiting on a screenshot of the actual face thumbnail from the user to
attempt a visual match against watch-face-wearfit's dial pool. No conversion started yet.


2026-07-16 (cont.) — "Red Magic" (chronos.ke, sernason, 360x360) converted, built, flashed
------------------------------------------------------------------------------------------------
Abandoned the watch-face-wearfit/GitHub angle above — found `chronos.ke/dials` instead, fbiego's
own community dial-sharing site referenced by the official Chronos FAQ ("Online watchfaces are
provided by the user community"). It's a real browsable+searchable gallery (1168 dials, plain
`?width=&height=&page=N` GET pagination, no JS needed), unlike the numeric-only GitHub repo. Did
find two actual Casio-branded faces there (RafSad's G-Shock-style "CASIO" and GudWin's
Edifice-style "Casio", both 360x360) — neither was confirmed as the exact one the user originally
saw in-app, but user picked a third face instead: **"Red Magic" by sernason** (360x360, tagged for
"X16 Pro"), share md5 `2582a02d9de4fc9ebae2abe3639dae95`.

**Download API gotcha:** `chronos.ke/api/dials/download?md5=...` 403s ("No authorization key") on
a bare request — not a real auth token, just needs a same-session cookie: `curl -c/-b cookies.txt`
to GET the `?share=<md5>` page first, *then* hit the download endpoint with that cookie jar +
matching `Referer`. Works with plain curl once sequenced right, no browser needed.

**Conversion pipeline generalized beyond the 75_2_410 precedent.** That script hand-transcribed
the whole watchface C file into a Python template — fine for 75_2's 5 elements, not viable for Red
Magic's ~50. New script `tools/facegen/gen_red_magic_410.py`:
- Asset scaling (decode/resize/re-encode each frame) reused the same approach as 75_2_410's
  `process_asset_file`, generalized to auto-discover every file in `assets/` via `glob` instead of
  a hardcoded filename list.
- The "logic" `.c`/`.h` (element creation, positions, update functions) is regex-rewritten from
  bin2lvgl's own generated output rather than retyped: every `lv_obj_set_x(ident, N);` /
  `lv_obj_set_y(ident, N);` gets its N scaled (y also gets `+Y_OFFSET`), `lv_img_set_pivot`/
  `lv_image_set_pivot` calls scaled on both axes, everything else (rotation formulas, digit-group
  indexing, update_* function bodies) left untouched since those aren't position-dependent. Should
  generalize to future faces with many more elements than 75_2_410 ever needed to handle.
- **Identifier-rename bug hit and fixed immediately:** first attempt used
  `re.sub(r'\bred_magic\b', ...)` for the `red_magic` → `red_magic_410` rename — never matched,
  because every real occurrence is `..._red_magic_...` and `_` counts as a word character, so
  there's no `\b` boundary between `face_` and `red_magic`. Fixed to a plain `str.replace()` (safe
  here since `str.replace` doesn't re-scan its own output, so `red_magic` → `red_magic_410`
  doesn't loop).
- **This time no `.header.always_zero` vs `.header.magic` issue** — the 75_2 gotcha didn't recur;
  every asset file bin2lvgl generated for Red Magic already used `.header.magic =
  LV_IMAGE_HEADER_MAGIC` directly. Also confirmed the generated logic file's LVGL-8-style calls
  (`lv_img_create`, `lv_img_set_src`, `lv_obj_clear_flag`) compile fine as-is under this project's
  LVGL 9.3.0 — matches the pattern already present in `digital_360.c` and other working faces, so
  left unrenamed (LVGL 9 clearly ships back-compat aliases for these).
- Validated the scale+letterbox result *before* spending a build+flash cycle by writing a quick
  standalone PIL compositor (parses `lv_obj_set_x/y` + initial `lv_img_set_src` out of the
  generated `.c`, decodes+pastes each asset at its position onto a 410x494 canvas) — caught layout
  bugs would have shown up here, none did; proportions and letterbox bars matched the original
  360x360 preview.

**Wired in:** `ENABLE_FACE_RED_MAGIC_410` added to `hal/esp32/app_hal.h`'s `ESPS3_2_06` block;
`init_face_red_magic_410`/`update_check_red_magic_410` appended to `ui_watchfaces_init()`/
`ui_update_watchfaces()` in `src/ui/ui.c` (same pattern as `75_2_410`/`classic_410`). No manual
`#include` needed — `support/header_gen.py` walks `src/faces/**/*.h` and regenerates
`src/common/generated_features.h` on every `pio run`, which is why that file always shows as
diff-noise post-build (`git checkout -- src/common/generated_features.h`, as previously noted).

**Built + flashed clean:** `pio run -e lolin_s3_2_06` succeeded (Flash now at 91.4% — getting
close to the ceiling, worth watching before adding more faces), `pio run -e lolin_s3_2_06 -t
upload` succeeded via PlatformIO's own bundled esptool (no need for the manually-installed
`~/.local/bin/esptool` this time — that was specifically needed for the raw `erase_region` nvs
operation before, not normal uploads). Serial boot log clean afterward: `Setup done`, `v6.3
[fbiego]`, no exceptions. **Not yet visually confirmed on-device** — user is checking now.

New files: `src/faces/red_magic_dial.bin` (raw download), `src/faces/red_magic/` (bin2lvgl output,
360x360, `ENABLE_FACE_RED_MAGIC` — left disabled/unused, kept only as the scaling source),
`src/faces/red_magic_410/` (the real face, 410x494 canvas), `tools/facegen/gen_red_magic_410.py`.
Nothing committed yet.


2026-07-16 (cont. 2) — Seconds hand added, weather wired up, three real bugs found+fixed
------------------------------------------------------------------------------------------------
User asked for two extras on top of the base port: (1) animate one of the 3 undecodable
"Analog x5" elements as a seconds hand, (2) wire up the top-left temperature readout (previously
static placeholder digits) to real weather data in Celsius.

**Confirmed via source, not guessing, that the 3 "Analog x5" elements are NOT recoverable
hour/min/sec hands.** User reasonably asked whether the moving-hand addition might reveal the
other two were originally hour/minute hands. Found real Kotlin source (not just the compiled jar)
at `~/esp32-lvgl-watchface/src/faces/bin2lvgl.kt`: its type table has `0x0D -> Item(true, "Analog
hands")` (genuine clock hands, fully *supported*) as a distinct type from `0x1A -> Item(false,
"Analog x5")` (what Red Magic actually uses, *unsupported*). Since bin2lvgl converts 0x0D
successfully whenever it's present, the fact all three came through as the different/unsupported
0x1A type rules out the hands theory — they're something else (probably an animated ring/pulse
effect tied to the two red gauge rings already baked into the static background art), format
undocumented anywhere found. So the added seconds hand is a genuine synthesized addition, not a
recreation — but its *position* (180,180 in source-space, dead center) is real, recovered straight
from the items.txt pivot coordinates that survived even though the graphic itself didn't convert.

**Seconds hand implementation:** new asset `assets/face_red_magic_410_dial_img_seconds_hand.c` —
hand-drawn (PIL, tapered red gradient + counterweight dot, 8x88px, pivot at (4,78)) rather than
recovered, since the source format for the original elements is unknown. Wired exactly like
`75_2_410`'s existing hands: `lv_obj_set_x/y` positions the pivot at dial-center (205,247 —
180,180 scaled + Y_OFFSET), `lv_image_set_pivot(obj, 4, 78)`, rotation driven by
`lv_image_set_rotation(hand, second * 60)` in `update_time_red_magic_410` (LVGL9 rotation units
are tenths-of-a-degree, so 60 = 6.0°/sec = one full turn per minute).

**Weather: three real bugs, all now fixed, worth remembering individually:**
1. **Wrong unit conversion.** First guess (from a bad on-device reading of "72°C") was that
   `watch.getWeatherAt(0).temp` must be Fahrenheit needing conversion. Wrong — checked the
   *working* `1167` face's `update_weather_1167()`, which uses `temp` completely raw with no
   conversion at all (plus a dedicated hidden minus-sign glyph for negative values, an asset
   `red_magic`'s source dial doesn't have an equivalent of) and correctly shows sane values on
   the same hardware. So `temp` is already Celsius — the "72" was actually caused by bug #2 below,
   and "fixing" it with F→C math on already-correct data made things worse. Reverted to raw
   `temp`, clamped to 0 on negative (accepted limitation — no sign glyph available here).
2. **Hundreds/ones digit positions reversed.** `items.txt` has the three temp-digit objects at
   x=70/60/50. Cross-checked against this same file's own working kcal-digit code (x=238/228/
   218/208 mapped ones/tens/hundreds/thousands) — higher x = lower place value. First attempt had
   this backwards (assigned hundreds-place math to the x=70/highest object, ones-place math to
   x=50/lowest, and hid the wrong one when <100), which is what actually produced the garbled
   "-0-"-looking output, not corruption — decoded and visually inspected the underlying
   `0123456789` glyph strip directly to rule out a repeat of the old planar-decode corruption bug;
   it's clean. Fixed the index/hide mapping to match the proven x-position convention.
3. **°C/°F label glyph never selected.** Two pre-baked label images exist at the same position
   (`face_red_magic_410_13_133026` and `_14_136230`); decoded both directly to identify which is
   which (no metadata for this in bin2lvgl's output) — 13_133026 is "°C", 14_136230 is "°F". Both
   were being drawn with neither hidden, so whichever was created later in the object list (°F)
   sat on top. Now explicitly shows the °C one and hides the °F one.

**Fourth, unrelated bug found from a separate user report** ("your analog and red magic [faces]
appear to have bitmaps too large... swipe... just moves the image within its own frame"): the
face-picker's thumbnail card (`addWatchface()` in `ui.c`) is a **fixed 160x160** slot regardless
of a face's actual canvas resolution — every correctly-behaving `_410` face (`174_410`, `1167_410`,
`2151_410`, ...) keeps its `..._dial_img_preview` asset at the original unscaled 160x160 and only
scales the main-canvas assets. Both `tools/facegen/gen_75_2_410.py` and
`tools/facegen/gen_red_magic_410.py` scaled *everything* uniformly including the preview, which
overflowed that fixed slot (75_2_410: 160→273px; red_magic_410: 160→182px) and produced exactly
the reported symptom. Fixed both scripts to exempt any filename containing "preview" from scaling,
and regenerated just that one asset in each face (hand-edited `.c`/`.h` logic files in both faces
were untouched by this — only the affected asset file was touched/replaced).

**Gotcha hit re-doing this:** re-running `gen_red_magic_410.py` to fix the preview scaling
regenerated `red_magic_410.c`/`.h` from scratch from the *original* unmodified `red_magic.c`/`.h`
in `src/faces/red_magic/` — silently wiping every hand-edit made after the first generation (the
seconds hand and all three weather fixes above). Had to reapply all of them a second time by hand.
**If touching this face again: either avoid re-running the generator once hand-edits exist, or
special-case what needs preserving before doing so** — it is not currently idempotent/safe to
re-run once the logic files have been hand-extended beyond what the script itself produces.

Built + flashed clean both times (once for the initial two features, once more for the three
follow-up fixes). Flash usage actually *dropped* after fixing the oversized previews (was
8689504 bytes, now 8576576) — a helpful side benefit given it was already at 91%+. Not yet
visually re-confirmed on-device as of writing — user is checking now.


2026-07-16 (cont. 3) — two more rounds of feedback, both fixed without a confirming photo
------------------------------------------------------------------------------------------------
Picker fix and temperature both confirmed working by user. Two remaining issues:

1. **Speckled artifact just left of the tens digit.** That position is exactly where the
   always-hidden-below-100 hundreds-place object sits (`face_red_magic_410_12_138312`) - its
   glyph strip was already confirmed clean by direct decode, so `LV_OBJ_FLAG_HIDDEN` alone
   apparently isn't fully suppressing it on this object (root cause not confirmed - no photo
   available to dig further). Fixed with a belt-and-suspenders approach: physically move the
   object to `x = -100` (fully off-canvas) whenever it'd be hidden, on top of the existing
   HIDDEN flag, restoring `x = 57` if it's ever actually needed (realistically never, for
   Celsius - see next point).
2. **Seconds hand a few px off the reticle.** Tried to nail this precisely without a photo by
   decoding the background asset directly and measuring the reticle graphic's actual red-pixel
   centroid: (207, 204) vs the hand's pivot at (205, 205) - only ~2px right/1px up, and in the
   *opposite* direction from what the user described ("down and left"). Sent an annotated
   crosshair image to cross-check, but user couldn't easily get a return photo, so went with
   trusting the direct on-device visual report over the indirect asset-only measurement: nudged
   the hand's object position from (201,169) to (198,172) - 3px left, 3px down. Unconfirmed as of
   writing; worth a precise photo-based re-measure next time one's easy to get, since the two
   signals disagreed.

User also raised a good design point worth recording: the 3-digit-wide temp readout (vs. 2 digits
+ sign on the working `1167` face) only makes sense for Fahrenheit (100+°F is realistic weather;
100+°C isn't) - and this dial does have a genuine unused `°F` label glyph sitting right next to
the `°C` one (see the "three real bugs" entry above). But there's no live Fahrenheit data path
into this firmware to wire it to either way - `watch.getWeatherAt(0).temp` is always Celsius
regardless of any phone-side app preference (confirmed via `1167`'s working code) - so hiding the
hundreds digit below 100 unconditionally is already correct as-is, nothing further to build here
unless that protocol gains a real F/C flag someday.

Built + flashed clean (boot log verified). Not yet visually re-confirmed on-device.


2026-07-16 (cont. 4) — a real photo settles both open questions
------------------------------------------------------------------------------------------------
User sent an actual close-up photo (rotated 90 deg from natural UI orientation - `img.rotate(-90,
expand=True)` corrects it) confirming temp digits now read correctly, but revealing two more
things:

**The "speckle" was a second, previously-missed real bug, not corruption.** Zooming the photo
showed a genuine solid white dash rendering just left of the temp digits, not JPEG noise. Traced
it to `items.txt` rows 8/9/15 - three tiny 8x2/6x2 elements at the *same x columns as the temp
digits* (x=70/60/50, one per digit slot) using bin2lvgl type `0xFA` ("Weather no+label" -
*supported*, unlike the Analog elements). These are a genuine minus-sign mechanism, conceptually
the same idea as `1167`'s dedicated sign glyph but implemented as an always-rendered dash
literally stacked under each digit position rather than a single separate object. They were
invisible before because the always-visible digit glyphs drawn on top fully covered them - moving
the hundreds-place digit off-canvas (the previous fix) exposed the dash underneath it
(`face_red_magic_410_15_0`) for the first time. Fixed properly rather than just re-hiding it:
digit math changed to use `abs(temp)` for the digits and show/hide this dash based on the actual
sign of the unclamped `temp` - real negative-Celsius support now, not just a hidden artifact
fixed. (The other two dashes, under the ones/tens slots, stay permanently covered since those
digits are never hidden - no equivalent issue there.)

**Hand position: photo confirmed the direction, asset measurement gave the precise value.**
Measured the photo directly (bright pivot-dot centroid vs. the ring's dark-hole centroid, after
correcting the photo's rotation first) and confirmed the pivot really was down-left of the true
center - the same direction the user originally reported, meaning the previous "trust the user
over the measurement" 3px adjustment had actually been applied in the *wrong* direction (no
photo existed yet to catch this). Tried to get an exact pixel delta from the photo itself but the
only convenient in-frame scale reference (the reticle ring, ~14 canvas-px / ~140 photo-px
diameter) is too small for the ~4px photo-pixel picking noise to give a trustworthy magnitude -
a small error in a 14px measurement swings the derived scale factor a lot. Went with the
direction-confirmed-by-photo, magnitude-from-the-original-precise-asset-decode approach instead:
target pivot = canvas (207,246) (measured directly from the background asset's red-pixel
centroid, no photo/lens/JPEG noise involved) -> `lv_obj_set_x/y(203, 168)`, i.e. barely 2px
right / 1px up from the very first (pre-user-feedback) placement of (201,169). Net effect: this
undoes the earlier wrong 3px-left-down nudge and applies the small correction the asset math
suggested all along. Unconfirmed as of writing - the "confirm with a real photo before adjusting
again" lesson from earlier in this session held up: guessing from words alone moved this the
wrong way once already.

Built + flashed clean (boot log verified, including a `[DEBUG]: Connected` BLE line same as
after the very first flash of this face). Not yet visually re-confirmed on-device.


2026-07-16 (cont. 5) — new face: citizen_410, first hand-authored-from-a-photo watchface
------------------------------------------------------------------------------------------------
User supplied a product photo (Citizen Eco-Drive "Red Arrows" Skyhawk pilot watch,
`CitizenE-DriveRedArrowsRAFA_900x.webp`, 900x900 studio shot on white) and asked for it as a
watchface, explicitly scoped as an incremental build: "grab a raster image of the watch (inc.
outer black dial) and we build this up over time. Start with Analogue hands, maybe put month day
number and 3 letter week day in the right hand lcd window". So unlike every other face so far,
this one's background is a real photo, not bin2lvgl output or hand-drawn primitives - new
territory, new gotchas below. Only the dial + fresh hands + date window are wired up; the
bezel scales, UTC sub-dial, left LCD window, and branding text are all still just static photo
content for now, as scoped.

**Photo processing pipeline** (`/tmp/.../scratchpad`, one-off Python/PIL/OpenCV, not committed
as a repo tool since it's unlikely to be reused verbatim for a different photo):
1. Located the bezel circle by eye against a coordinate-grid overlay (automatic edge/width
   detection kept getting confused by the crown/pusher buttons protruding past the true circular
   edge on one side) - settled on center (432,398), radius 253 in the original 900x900 photo.
2. **Removed the photo's own baked-in hands** so they wouldn't duplicate/clash with the fresh
   rotating hands drawn on top: `cv2.inpaint` (TELEA algorithm) with a hand-traced mask. First
   couple of attempts had the red chronograph second hand's mask in completely the wrong place -
   turned out that hand isn't a simple straight line from center, it has a white paddle-shaped
   *counterweight* on the opposite side from the pointing tip (common chronograph seconds-hand
   design), which a naive "line from center to where I thought the tip was" mask entirely missed.
   Found the true path by a polar/radial red-pixel scan from the center (had to restrict the
   angle search to avoid false-matching the several *other* red elements on this dial - the "9"
   numeral, "RED ARROWS" branding, "AL-1"/"AL-2"/"TIME" text - which a naive whole-image red
   threshold kept latching onto instead of the actual hand). Final result: hour/minute hands
   cleanly removed, red hand and its counterweight cleanly removed; one small soft blur artifact
   remains where the hour hand crossed the tiny UTC sub-dial/CITIZEN-logo text (inpainting
   struggles to reconstruct fine text it has no surrounding context for) - tried several more
   inpaint radius/algorithm/mask-width combinations to fix it, none improved on the first
   reasonable result, so left it - it's small and much less noticeable at actual 410px watch size
   than in a zoomed-in crop. Revisit later if it bothers the user; not worth more time now per
   proportional-effort judgement.
3. Circular-masked onto black (feathered via 4x supersampling) and scaled to fill the 410-wide
   canvas → 410x410 (this dial is round enough that letterboxing top/bottom like classic_410's
   410x462 made no sense here; full square fits naturally).
4. Encoded as a plain opaque `LV_COLOR_FORMAT_RGB565` image (no alpha needed - the masked-out
   area outside the circle is already pure black RGB, which matches the canvas's black background
   exactly, so real alpha transparency would be redundant work for zero visual difference).
   **Preview thumbnail generated separately at native 160x160** from the start this time -
   learned that lesson properly after hitting it twice already (75_2_410, red_magic_410).

**Face code**: followed `classic_410`'s hand-authored pattern (real prior art in this repo for a
non-bin2lvgl face) rather than anything bin2lvgl-shaped - `lv_image_create`/`lv_image_set_src` for
the static photo background instead of canvas-drawn primitives, but the *hands* and *date label*
are lifted near-verbatim from `classic_410`'s approach (lv_line objects with trig-computed
endpoints recalculated every `update_time_citizen_410()` call, `lv_label_set_text_fmt("%s %02d",
DAY_NAMES[weekday%7], day)` for the date) since that part of the technique is completely
face-agnostic. Simplified from Classic's hands (no red center "stripe" overlay - this dial's
photo hands are plain white/silver) and added a third hand (Classic's red hand IS the second
hand; Citizen needed hour+minute in white/silver plus a separate thin red second hand, since the
source photo has three hands not two). Dial center measured directly from the crop construction
(cropped centered exactly on the bezel, so center is exactly (205,205) in the 410x410 image by
construction, not by measurement/guesswork this time). Date window position (canvas x
270-345,y207-243, covering the photo's original "TYO/009/35" LCD box) measured directly off the
processed 410x410 image via the same coordinate-grid-overlay technique used throughout this
session - painted over with a plain dark box (matching classic_410's date_box style) before
adding the label, so the new dynamic date doesn't sit on top of the photo's old static "TYO 009"
text.

**Same two gotchas from earlier in this session recurred and were caught immediately this time**
(the payoff of having them written down): forgot `#include "../citizen_410.h"` in both new asset
files at first (compile error, `lv_img_dsc_t`/`LV_IMAGE_HEADER_MAGIC` undeclared - same exact
failure mode as the red_magic_410 seconds-hand asset); and generated the preview at native
160x160 *from the start* rather than accidentally scaling it, avoiding a repeat of the
picker-thumbnail-overflow bug.

Wired into `hal/esp32/app_hal.h` (`ENABLE_FACE_CITIZEN_410`) and `src/ui/ui.c` same as every
other face. Built clean. **Flash usage now 94.3%** (was 91.4% before this face - a single
410x410 uncompressed RGB565 photo is ~336KB, by far the largest single asset in this project so
far) - getting close to the ceiling, worth being conservative about adding more full-photo-based
faces without a compression strategy. Flashed clean, boot log otherwise normal but showed one
`Failed to allocate buffer, retrying` line that didn't repeat or crash anything (BLE connected
fine afterward, normal screen-timeout behavior followed) - plausibly related to handling this
much bigger image than any previous face, worth watching for if it recurs or gets worse as more
gets added to this face, but not treated as a blocker for this first iteration. Not yet visually
confirmed on-device - user is checking now. Next steps per the user's own scoping: nothing
specific requested yet beyond this first iteration (dial + hands + date) - wait for feedback
before touching the UTC sub-dial, left LCD window, or bezel-scale complications.


2026-07-16 (cont. 6) — citizen_410 quality pass: corrected crop, real hand sprites, LCD font crash
------------------------------------------------------------------------------------------------
User reviewed the first citizen_410 iteration and asked for real improvement, not just tweaks:
(1) the circle crop clipped black bezel on the right and showed white background on the left,
(2) wanted actual raster hand images cut from the photo and animated, rather than drawn lines,
properly centered, (3) wanted the date window's background replaced with matching LCD
color/texture, same font as the watch's real LCD, black text with shadow, day on top/date below,
centered.

**Crop fix**: the single-circle fit (cx=432,cy=398,r=253) was measured by eye and came out ~10px
too far left - explains the exact symptom (right side clipped, since true right edge needed
more margin than the wrong-centered circle gave it; left side had ~15px too much margin,
showing background). Fixed by measuring each of the 4 edges independently via tight zoomed
crops (avoiding the crown/pushers, which extend past the true bezel circle on the right and
kept skewing single-circle-fit attempts) rather than eyeballing one circle: corrected center
(442,401), radius 248 - visually confirmed tight against the bezel all the way around this
time.

**Real hand sprites - much harder than expected, multiple false starts:**
- First attempt reused the *inpainting* mask's line coordinates+widths for extraction too.
  Result: badly contaminated sprites (bits of "RED ARROWS" branding, bezel numerals, chapter-ring
  ticks). Root cause: those masks were deliberately wide (18-24px) for inpainting, where a wide
  margin for angular error doesn't matter since it's just papering over removed pixels - but for
  *extraction* with a precise thin mask, the same imprecise angle became immediately visible as
  "captures wrong stuff nearby" rather than "reliably covers the real hand."
- Several rounds of re-tracing hand tip coordinates by eye (even with grid overlays) kept
  producing masks that visibly missed the real hand path when checked against a mask-overlay
  visualization - eventually traced to comparing against debug crops too small/wrong-angled to
  see the true hand extent. Also discovered the small red hand near "12"/the UTC text box is a
  *separate* hand belonging to the small 24-hour UTC sub-dial (pivoting at the sub-dial's own
  center ~(255,155)), not part of the main chronograph second hand - easy to conflate since both
  are red and both pass near the main dial center.
- What actually worked: wide, clear crops of *just* one hand's real extent at a time (not the
  whole dial), tracing tip coordinates directly off those, using much narrower widths matching
  the hands' true thickness (~15px hour, ~12px minute, ~4px second - vs. the 18-24px inpainting
  widths) so the extraction mask itself excludes most surrounding text by construction rather
  than relying on color filtering (hand color and bezel-text color are both white/silver -
  color alone can't distinguish them). Final sprites still carry minor artifacts right at the
  pivot end (small bleed from the UTC hand / center cap area) but those are hidden under the
  separate center-cap circle object in the assembled face, so not visible on-device.
- Sprites scaled by the same 410/496 factor as the background, wired up as `lv_image_create` +
  `lv_image_set_pivot` + `lv_image_set_rotation` (replacing the previous `lv_line`-drawn hands).
  Each sprite is cut from the photo at whatever angle it happened to be posed at (not pointing
  "up"), so each needs its own as-photographed base-angle subtracted before applying the real
  time-driven rotation - `HAND_HOUR_BASE_DEG`/`HAND_MINUTE_BASE_DEG`/`HAND_SECOND_BASE_DEG`,
  measured from the same traced tip coordinates used to cut each sprite.

**LCD window background**: sampled the real photo's own LCD-panel pixels (the exact region being
covered), heavily Gaussian-blurred to erase the old static "TYO/009/35" text while preserving
the panel's actual grey gradient/lighting/texture, used as `lv_obj_set_style_bg_image_src` on the
date box instead of a flat placeholder color - now blends into the surrounding dial photo
seamlessly instead of looking like a pasted-on box.

**Custom LCD-matching font caused a full crash-loop - reverted, real fix deferred.** Sourced
`DSEG14-Classic` (SIL OFL licensed, genuine 14-segment font - confirmed this dial's LCD really is
14-segment alphanumeric, not simple 7-segment, by zooming into "TYO" and seeing the classic
unlit-segment ghosting pattern) via `keshikan/DSEG` on GitHub, built Node+`lv_font_conv` from
scratch (nvm install, no sudo needed) since neither was on this machine, converted to LVGL C
format at 18px/4bpp. Compiled fine, but crashed immediately on every boot with an identical,
repeatable `Guru Meditation Error: LoadProhibited` at `EXCVADDR: 0x00000010` - decoded the
backtrace with `xtensa-esp32s3-elf-addr2line` (bundled with the PlatformIO toolchain, no download
needed) straight to `lv_draw_sw_letter.c:176`, a null-`draw_buf->data` dereference inside
`lv_font_get_glyph_bitmap()`'s caller - i.e. the font was returning no bitmap for a glyph LVGL
tried to draw. First fix attempt: realized the placeholder text `"???\n--"` used `?`/`-`
characters that weren't even in the font's converted range (only had space+digits+A-Z) - fixed
the placeholder text AND expanded the font's range to include them for safety. **Crash persisted
identically, same address, after that fix** - meaning missing glyphs weren't the (whole) story;
something deeper in this specific font conversion is incompatible with this LVGL build's
glyph-bitmap retrieval path (untested theories: bpp/compression option mismatch, static-bitmap
caching flag, LVGL version-specific font table layout). Given the watch was in an actual
crash-loop (unusable until reflashed) - not a cosmetic bug - treated getting back to a working
state as the immediate priority over continuing to debug blind: reverted the date box to
`&lv_font_montserrat_16` (proven-working elsewhere in this project), rebuilt, reflashed, confirmed
clean boot. **The DSEG14 font work (fonts extracted, `lv_font_conv` toolchain now installed and
working, `dseg14_bold_18.c` sitting in `assets/` unused) is preserved for a future, unhurried
attempt** - next time, test the converted font file in isolation (a trivial single-label test
face, not integrated into a real face) before wiring it into anything real, so a font bug doesn't
put a whole face's build at risk again.

Flash usage now 93.8%(ish, fluctuates slightly build to build) - real hand sprites plus the LCD
background patch added meaningfully to the earlier 94.3%. Getting genuinely tight; worth
compressing or trimming something before the next big asset addition to this face.

Built + flashed clean (Montserrat date font), boot log verified, BLE connected. Not yet visually
re-confirmed on-device - user is checking now.


2026-07-16 (cont. 7) — crop STILL off after "fixing" it once; second, more rigorous correction
------------------------------------------------------------------------------------------------
User reported the exact same symptom again after the previous "fix" (442,401,r=248): still
noticeable background on the left, still clipping black bezel on the right - just less severe
than the original (432,398,r=253). Confirms the second attempt moved in the right direction but
undershot.

Root cause of *both* previous misses: measuring the circle by eye against a coordinate-grid
overlay isn't precise enough for this - off by small amounts each time, in a way that "looks
right" on quick visual check but is wrong on either close inspection or the real device's higher
effective resolution. Also discovered along the way that automated single-threshold edge
detection doesn't work either: the right side has real ambiguity between "black bezel" (what we
want the crop boundary at) and "grey case metal" (further out, non-white but also not part of
the bezel) - a simple "is it non-white background" scan finds the case edge, not the bezel edge,
and the case is asymmetric (wider on the crown side).

**What actually worked this time**: tight, fine-pixel-gridded zoom crops (10x zoom, 5px grid
lines with coordinate labels) at each of the 4 cardinal edges individually, read by eye against
the labelled grid rather than estimating from a full-dial overview. This is slower per-edge but
each reading was internally consistent (left/right/top/bottom independently gave a radius within
1px of each other: ~254-255), which the two previous whole-image-eyeball attempts never achieved.
Also caught a second, previously-unnoticed error this same way: the earlier top-edge measurement
(153) was cutting into the bezel's outer *red ring*, which starts at y=140, not where the black
bezel numerals start (~155) - i.e. one of the earlier attempts wasn't just off-center, it was
also using the wrong bezel-edge definition entirely (excluding the red ring from what counts as
"the bezel"). Corrected circle: center (446,394), radius 254 (was 442,401,248; before that
432,398,253) - visually confirmed tight against the bezel on all 4 sides this time, red ring
included.

**Full re-derivation, not just a background recrop.** Because the hand sprites and LCD-window
patch are all measured relative to the crop's own coordinate frame, every corrected center means
re-deriving all of them, not just the background image:
- Hand tip coordinates: rather than re-tracing by eye a third time, converted the already-traced
  tip coordinates from the previous crop into *original-photo* coordinates (add the old crop's
  origin), then back into the *new* crop's local coordinates (subtract the new crop's origin) -
  since these tips represent real, fixed, physical points on the watch, this round-trip is exact
  and avoids repeating the error-prone manual tracing work a third time.
- Hand base-angles recomputed from those same shifted tip coordinates (trig off the new true
  center) rather than reused, since the previous base-angle numbers were relative to the old
  (wrong) center.
- LCD background patch: re-sampled and re-blurred from the new background image directly (window
  position re-measured off the new 410x410 image with the same grid technique, came out at
  roughly the same relative spot: 272-346,203-244, small few-px shift from before).
- All three hand sprites re-extracted, re-scaled by the new background's own scale factor
  (410/508 this time, vs 410/496 previously - the new crop is very slightly larger in source
  pixels since the radius grew from 248 to 254).

Built + flashed clean (Montserrat date font still in place - DSEG14 font issue from the previous
entry not yet revisited), boot log verified, BLE connected same as before. Flash usage 95.0%
(8961265 bytes) - marginally lower than the previous 95.0%/8969749 despite adding a slightly
larger background, likely because the new (correctly-sized) crop let some hand sprites end up
marginally smaller. Still very tight - at most one or two more meaningfully-sized assets before
this needs a compression pass. Not yet visually re-confirmed on-device - user is checking now.


2026-07-17 — citizen_410: new source photo, red-bleed-free minute hand, hand-length pass
------------------------------------------------------------------------------------------------
User supplied a fresh, higher-quality product photo (1008x1024 jpg, not the earlier 900x900 webp)
plus a GIMP-cleaned minute-hand layer (red bleed from the chronograph second hand overlapping the
minute hand in the photo, painted out by hand) and asked for the background, the minute hand, and
overall hand proportions to be redone from this new source.

**Background**: new 410x410 crop already supplied pre-made by the user (`citizen_bg_410_v2.png`
in scratchpad) - verified the bezel-vs-content geometry looked right (ellipse-fit on the outer
bezel edge came back essentially a perfect circle, axis ratio 1.001) before trusting it, then
swapped straight into `face_citizen_410_dial_img.c`/`_dial_img_preview.c`. Diffed >50% of pixels
against the previous background, confirming this is a real replacement, not a no-op.

**Minute hand - avoided re-doing the fragile photo-alignment tracing from scratch.** The GIMP
export (`minute_hand_clean.png`) came out as a 517x523 canvas with the real hand content occupying
only a small ~180x115 region inside it (confirmed via the alpha channel) - initial attempts to
template-match that content back against the source photo to recover an exact scale/position
transform gave unreliable, low-confidence results (correlation peaks landing on plainly-wrong
areas of the photo). Rather than burn more time on that, dropped the "recover the true photo
scale" goal entirely: found the hand's own pivot (rounded base end, identified by width profile
along its principal axis) and tip (tapered point) directly in the cleaned cutout's own pixel
space, then uniformly scaled+positioned it to hit a *chosen* target length in the 410-canvas
frame instead of an inferred one. This sidesteps the whole alignment problem - the hand's shape is
real photo content either way, only its final size in the canvas is a design choice, not something
that needs forensic recovery.

**Hand-length pass, all three hands.** User feedback on an early proportions preview: minute and
second hands were reaching almost to the bezel (way past the dial's main hour markers), hour
wasn't clearly shorter than the other two, said to fix so minute/second reach the *middle* of the
main hour markers and hour reads clearly shorter. Measured the real target radius by overlaying
concentric rings on the background and reading off where the big lume markers sit (~100-150px
from center) - picked 140px as the minute/second target and 95px for hour. Concretely:
- Minute: fresh cleaned cutout, uniformly scaled so pivot-to-tip = 140px. New pivot (12,82), new
  base angle 56.6 deg (recomputed from the cutout's own geometry, since this is a new crop with a
  different as-photographed angle than the old sprite).
- Hour: previous real sprite, uniformly scaled 109px -> 95px (pivot (75,71), base angle unchanged
  at 313.8 deg - a uniform scale doesn't change orientation).
- Second: previous real sprite, uniformly scaled 116px -> 140px (pivot (7,6), base angle unchanged
  at 180.8 deg).
Verified with a Python/PIL composite (decoding the real LVGL assets back to PNG, replicating the
exact pivot/rotation/base-angle math from `citizen_410.c`) at three test times (10:10:30, 12:00:00,
3:15:45) before touching the firmware - 12:00:00 in particular confirms z-order is right (hour
behind minute behind second, all three visible stacked when overlapping), which needed no code
change since it already falls out of LVGL's default "later-created child draws on top" behaviour
(hour_hand created first, then min_hand, then sec_hand).

**LCD date window moved.** While placing the new background, found the grey LCD-panel patch
(previously at canvas x272-346,y203-243) no longer lined up - the new photo's window sits further
left. Re-measured by eye against a fine pixel grid: real panel is x241-306,y202-250. Re-sampled and
re-blurred the LCD background patch from this new location, updated `DATE_BOX_X/Y/W/H` in
`citizen_410.c` to (244,205,60,43) (small inset from the true panel edges, same margin style as
before).

Regenerated all six changed assets through `tools/facegen/lvgl_img.py`'s
`encode_from_rgba`/`format_c_array` (same planar-RGB565(+alpha) format as every other hand-authored
asset in this repo - see the two gotchas logged earlier in this file). Built clean for
`lolin_s3_2_06`, flash usage 95.0% (8969489 bytes, essentially flat vs before). Flashed via
`pio run -t upload`, boot log confirmed clean (AXP2101 battery read fine, `Setup done`, no
crash-loop). Not yet visually confirmed on the actual device - user is checking now.


2026-07-17 (cont. 2) — citizen_410: third source photo (v3), rotation-error investigation
------------------------------------------------------------------------------------------------
User supplied yet another photo (`CitizenE-DriveRedArrowsRAF2.jpg`, same 1008x1024 as the v2
source) and asked to use it instead, plus flagged the face as looking "out by about 1.6 degrees"
and asked for confirmation+fix. This photo's main dial has **no baked-in hour/minute hands at
all** (only the small UTC and 24h sub-dials show their own static hands) - a real win, since it
means no inpainting is needed this time, unlike every previous source photo.

**Rotation investigation - the outer-bezel-edge fit is the wrong reference for this.** First
measured rotation the same way as the circularity check earlier this session (ellipse-fit on the
outer bezel edge) and got a plausible-looking but, it turns out, *wrong* center - cross-checking
against the "9"/"3"/"6" numerals (which by construction sit at exactly 270/90/180 degrees on a
real watch dial) with that center gave inconsistent, mixed-sign deviations (-0.5, +1.0, +1.2 deg)
that don't fit a single rotation value - the signature of a bad center, not a real rotation.
Root cause: this is the exact crown/pusher bias already logged earlier in this file for the v1/v2
crops - the bulges these add to one side of the bezel skew an edge-only circle/ellipse fit away
from the dial's true optical center.

**What actually resolved it**: jointly fit center (cx,cy) + rotation via least-squares against
the 3/6/9 numerals, then *independently validated* that fit against 8 more reference points (the
lume hour markers at 1/2/4/5/7/8/10/11 o'clock, not used in the fit) - residuals came back small
and centered near zero (mean 0.2 deg, std 0.44 deg) across all 11 points, which is a real,
over-determined confirmation rather than a 3-equations-3-unknowns coincidence. Conclusion: this
photo's actual content rotation is small, ~0.2-0.3 deg, not 1.6 - the "1.6 deg" figure look wasn't
reproducible from the dial geometry itself, using the same method on the *previous* (v2)
background gave the same small ~0.2-0.3 deg answer too. Corrected it to ~0.00 deg anyway (free to
fix once measured) by rotating the source photo by the fitted offset before cropping. **Flagged
to the user that if they still see a ~1.6 deg discrepancy after this, it's more likely coming from
a hand base-angle calibration error (measured by tip-tracing, a fuzzier process) than from the
background photo's rotation** - worth them checking hand-tip-vs-tick-mark alignment on the actual
device specifically, if the discrepancy persists, so that can be pinned down separately.

**Rebuilt background pipeline**: rotate source by fitted offset around the fitted center, crop a
998x998 square (radius 499) around it, resize to 410x410 - same recipe as every previous
iteration, just with a properly cross-validated center/rotation this time instead of a single
biased edge fit. LCD date window relocated *again* (third different position across three source
photos) - true panel this time measured at x250-303,y202-274 (taller than before), box kept
text-sized (53x44, same convention as every prior iteration) and top-aligned within it rather than
stretched to fill the taller panel. Hand assets (hour/minute/second, tuned earlier this session)
reused unchanged - verified their target lengths (140/140/95px) still land in sensible places
against the new background's marker positions (same physical watch, similar crop scale) before
committing to that.

Regenerated dial/preview/LCD-bg assets, rebuilt clean for `lolin_s3_2_06` (flash usage 95.0%,
8968993 bytes, flat), flashed via `pio run -t upload`, boot log clean (battery reads fine, no
crash-loop). Not yet visually confirmed on-device - user is checking now.


2026-07-17 (cont. 3) — citizen_410: bigger LCD font, day/date divider, left LCD battery %
------------------------------------------------------------------------------------------------
User asked to (1) grow the day/date font to fill the LCD window more, (2) add the horizontal
divider line the real watch has between day and date, (3) wire up the left LCD window (previously
untouched, still static photo content) to show battery %, same font size.

Went from `lv_font_montserrat_16` to `_20` for both windows (all sizes 8-48 are enabled in
`lv_conf.h`, so this was just a constant change, no new font work needed). Split the single
"\n"-joined 2-line date label into two separate single-line labels (`day_shadow`/`day_label` and
`date_shadow`/`date_label`) so a divider bar (`date_divider`, a plain 2px-tall `lv_obj`) could sit
precisely between them - couldn't do that cleanly with one auto-wrapped multi-line label. Grew the
date box 53x44 -> 53x54 to fit the bigger font without crowding (true panel allows up to y271, so
there was headroom).

**Left LCD window measured for the first time** (x95-158,y200-231 true panel) - inset the box to
x100-154,y203-228 specifically because a small red gauge-pointer element (part of the
USA/EUR/JOU/CHN sub-gauge) sits just outside the panel at x85-93 and was bleeding red into the
blurred background patch on a first attempt (margin=10 blur sample pulled it in) - fixed by
shrinking the blur sample margin to 3px, tight enough to avoid the red but still enough to erase
the panel's original static content. New `face_citizen_410_lcd_bg_left` asset (54x25), new
`batt_box`/`batt_shadow`/`batt_label` objects, same shadow-emboss style as the date window.
`update_status_citizen_410()` was already being called with the real `watchBatteryPercent` value
(wired up back on 2026-07-12, just never displayed anywhere in this face) - implemented it to set
the new label text (`"%d%%"`) instead of the empty stub it had been since this face's creation.

Sanity-checked the layout before flashing by approximating the label positions/font size with a
non-LVGL TTF (KaTeX Bold, closest thing available on this machine, not a real montserrat match) at
20px over the actual background+LCD-patch composite - confirmed neither label overflows its box
and the divider sits in a sensible place, before trusting it on real hardware.

Built clean (flash usage 95.1%, 8973549 bytes - the extra text label/font/asset cost ~4KB), took
notably longer to build than usual (~169s vs the usual ~35-40s) - full FrameworkArduino rebuild,
unclear why (header touched was this face's own, shouldn't affect framework compilation units);
not investigated further since the build still succeeded and this isn't the first time PlatformIO
has done a wider rebuild than expected on this project. Flashed via `pio run -t upload`, boot log
clean (battery read 29%, matches what should now be showing on the left LCD). Not yet visually
confirmed on-device - user is checking now.


2026-07-18 — citizen_410: minute hand pivot/counterweight bug fixed
------------------------------------------------------------------------------------------------
User feedback after checking the 2026-07-17 build on-device: minute hand pivot point was wrong,
counterweight possibly erased, second hand too long and unreadable (looks black, should be red),
hour hand barely visible, left LCD battery/date layout requests. Also asked whether the original
source images were still accessible for re-diagnosis - they were, in two old session scratchpads
under /tmp/claude-1000/-home-greg/{ae1adad1,d2df0337}.../scratchpad/ (source photos + the GIMP
minute-hand cutout + every intermediate mask/grid PNG from the original dial-fitting work).

**Root cause found by decoding the current on-flash sprite back to PNG** (via a standalone
RGB565+alpha decoder matching tools/facegen/lvgl_img.py's planar layout, since that shared script's
regex only matches the `_data_0[]` naming convention used by the 75_2_410 generator, not the
hand-authored `_data[]` arrays in this face - didn't touch the shared script, just wrote an
equivalent one-off parser). Confirmed two real bugs, not just a perception issue:
- **Zero red pixels** in the shipped minute-hand sprite (verified against 381 red pixels in the
  original `CitizenE-DriveRedArrowsRAFMinute2.png` cutout) - the red pivot-hook counterweight was
  fully lost.
- **The black base cap and the blade are two disconnected pieces** with a visible gap between them,
  and the previous session's pivot (12,82) landed inside the disconnected cap, nowhere near the
  blade - a visible artifact of the "find pivot and tip independently in the cutout's own pixel
  space" approach used to sidestep the alignment problem the day before: that approach implicitly
  treated the cap and blade as one shape but the actual extraction/positioning step that produced
  the shipped sprite split them.

**Found the true pivot by inspection, cross-checked numerically.** Zoomed into the original cutout
at 6x and found an actual small screw-hole visible at the center of the red hook loop, at ~(257,259)
in source-image space - this is a real mechanical feature (the axle the hand pivots on), not a
guess. Cross-checked against a width-profile scan along the shape's principal axis (same method
used 2026-07-17 for the hour/second hands): the widest point of the base region landed at ~(255,261)
- 2-3px of the by-eye estimate, good agreement between two independent methods.

**Rebuild**: connected-component check on the original cutout confirmed it's genuinely one piece
(4134px single component, some sub-10px anti-aliasing specks) - so re-cropped the *entire* opaque
region as a single crop (not a re-derived mask), keeping cap+hook+blade+color intact. Farthest
opaque point from the new pivot is the tip, at source-space distance 162.29px; uniformly scaled the
whole crop by 140/162.29 (0.8627x) to preserve the existing 140px pivot-to-tip target (unchanged -
length wasn't touched here, that's a separate open item since the user also flagged the second hand
as too long and wants both re-measured against the inner bezel tick ring, not done yet). New sprite
150x100, new pivot (31,76). Orientation preserved exactly (no rotation applied, pure crop+scale of
the same connected shape), so the existing HAND_MINUTE_BASE_DEG (56.6 deg) needed no change.

**Verified two ways before flashing**: (1) visually, marking the new pivot on a 4x zoom of the
rebuilt sprite - lands exactly on the screw-hole in the middle of the red hook, cap and blade fully
connected, red intact; (2) a Python/PIL composite against the real (non-preview, 410x410) dial
background at 12:00:00 and ~10:10 - hand points at the correct tick positions at both test angles,
confirming the preserved base-angle math still holds after the recrop.

Updated `citizen_410.c` min_hand pos/pivot from (12,82)->(31,76) to match. Built clean, flash usage
unchanged (~95%), flashed via `pio run -t upload`, boot log clean via a manual RTS-toggle reset read
(ESP-ROM banner through "Starting up device", no panic/backtrace).

User confirmed the direction was right and gave more feedback in the same pass: second hand too
long and reads mostly black (should be red), hour hand barely visible, plus a batch of LCD layout
requests (drop the "%" on battery and shift it over, grow+lower the date number, and - after asking
- switch the day/date/battery font from Montserrat to the dseg14_bold_18 LCD font that's been sitting
unused in this face's assets since it was generated. Also clarified second-hand-vs-hour-hand are two
separate bugs, not one being mistaken for the other.

**Second hand rebuilt the same way as the minute hand, from the *original* full studio photo**
(`CitizenE-DriveRedArrowsRAFA_900x.webp`, not the v2/v3 crops - the hour/second hands were never
migrated off this original photo, only the background and minute hand were). Confirmed the same
category of bug as the minute hand: the shipped sprite decoded to mostly near-black RGB for most of
its shaft (only tip and base ends kept real red), most likely a color-extraction artifact from the
original "polar/radial red-pixel scan" approach struggling on a 1-2px-wide red line against a dark
background. Re-extracted using a red-pixel mask + local dilation (to catch the black outline and the
screw-hole neighbourhood) + connected-component filtering (keeps just the needle+hook island, drops
the nearby AL-1/AL-2/TIME red text a plain bounding-box crop would've caught) - this preserves the
real photographed red throughout, not just fragments of it. Found the true pivot the same way as the
minute hand: a visible screw-hole at the centre of the red hook counterweight decoration, this time
at crop-local (85,69). Uniformly scaled to the same 140px pivot-to-tip target as the minute hand
(user confirmed that reach is accurate against the inner bezel tick ring, so re-used it rather than
re-deriving from scratch). New sprite 105x178, new pivot (70,54).

**Base angle recomputed, not reused** - unlike the minute hand, this crop's pivot-relative
orientation doesn't match the old sprite's, so the old HAND_SECOND_BASE_DEG (180.8) would point the
wrong direction. Computed the new native angle geometrically from the crop's own pivot->tip vector
(208.8 deg) and verified it two ways: (1) cross-checked the same formula against the *unchanged*
minute hand base angle (56.6) and it reproduced 57.6 within rounding - consistent; (2) built a
proper LVGL-accurate composite check this time. First attempt at this reused a Python/PIL rotate()
call directly with the LVGL rotation value, which is wrong - PIL's rotate() is counter-clockwise-
positive, LVGL's lv_image_set_rotation is clockwise-positive, so that first composite had the hand
sweeping backwards (caught it by testing 4 angles instead of 1-2: three didn't land where a real
clock hand would). Fixed by negating the angle before calling PIL rotate(); re-tested at all four
cardinal positions (0/90/180/270 = 12/3/6/9 o'clock) against the real dial background and all four
landed correctly this time. Worth remembering for any future from-scratch hand rebuild: verify a new
composite-check script against >=3 non-trivial angles, not just 0 deg, since 0 deg can look right by
coincidence regardless of rotation sign.

Updated `citizen_410.c` sec_hand pos/pivot (7,6)->(70,54) and HAND_SECOND_BASE_DEG 180.8->208.8.
Built clean, flashed via `pio run -t upload`, boot log clean (RTS-toggle reset read, ESP-ROM banner
through "Starting up device", no panic/backtrace).

**Hour hand rebuilt too, same "dim instead of bright" bug as the second hand's color problem, plus
contamination.** Decoded the shipped sprite and found it averaged RGB ~30-150 (a dim muddy gray)
across its whole visible area, never reaching real white, plus red bleed near the pivot from the
neighbouring second-hand hook - explains "only some pixels available when I look hard" exactly:
against a black dial, a dim gray hand is nearly invisible except its brightest anti-aliased edges.
Root cause looks like the same class of bug as the other two hands: a lossy extraction from the
original studio photo (`CitizenE-DriveRedArrowsRAFA_900x.webp`) that didn't preserve real
brightness/color.

Re-extraction was harder than the other two hands because white isn't a distinctive color on this
dial (numerals, lume markers, tick marks, and the "USA/EUR/JIC/CHN" city-selector sub-dial text are
all white too, and that sub-dial sits immediately next to the hour arrow's base) - a plain
white-threshold + dilate + connected-component pass kept bridging to that sub-dial text no matter
how the crop was framed. Fixed by two passes: first an *undilated* white mask to get clean,
unbridged fragments, filtered out any fragment whose bounding box fell in the known text zone
(sat with a fixed pixel offset from the sub-dial's own known position), then dilated only the
kept fragments to re-merge the arrowhead's own pieces (it's visually split into several bits by the
hand's window-cutout black divider line) without re-bridging to the text.

**The connecting tail down to the pivot couldn't be extracted at all** - traced it in the source
photo and that whole region actually is a thin near-black tail against dark dial texture, below any
reasonable extraction threshold, not a lossy-extraction artifact this time. Since a black tail
against a black dial is visually indistinguishable from transparent-showing-black-dial, filled it
with a small synthetic dark-grey polygon bridging the arrowhead's base to the mechanically-correct
pivot point instead of trying to trace individual real pixels - same visual result, guarantees the
hand actually rotates around the true dial centre rather than leaving the bright arrowhead
disconnected from its rotation point (checked this mattered: an early attempt using the pivot
implied by the extracted fragments alone left the arrowhead floating ~50px from where the tail
fragment ended).

Pivot placed at the shared dial-centre offset (this hand's crop was framed so that point falls
inside the canvas) rather than a screw-hole - unlike the other two hands, no screw-hole is visible
for this one, it's hidden under the arrowhead itself in the photo. Uniformly scaled to the existing
95px pivot-to-tip target (hour hands are shorter than minute/second by design, target unchanged).
New sprite 107x74, new pivot (94,61).

**Base angle recomputed** (306.3 deg, not the old 313.8) for the same reason as the second hand - a
fresh crop doesn't share the old sprite's pivot-relative orientation. This time verified the
composite check properly from the start (the second-hand rebuild caught a PIL-vs-LVGL rotation-sign
bug the hard way): rotate() angle negated to match LVGL's clockwise-positive convention, checked at
0/90/270 deg (12/3/9 o'clock) against the real dial background - all three landed correctly.

Updated `citizen_410.c` hour_hand pos/pivot (75,71)->(94,61) and HAND_HOUR_BASE_DEG 313.8->306.3.
Built clean, flashed via `pio run -t upload`, boot log clean (RTS-toggle reset read, ESP-ROM banner
through "Starting up device", no panic/backtrace).

**LCD layout/font pass, all four remaining items done together** since they're coupled (font swap
changes the metrics the other three depend on): switched `DATE_FONT`/`BATT_FONT` from
`lv_font_montserrat_20` to the `dseg14_bold_18` LCD-style font that's been sitting generated but
unused in this face's assets since it was first added - real 14-segment digital-display look,
user confirmed wanting the swap earlier this session. Battery: dropped the trailing "%" from both
`lv_label_set_text_fmt` calls (init and `update_status_citizen_410`), and added a
`BATT_EXTRA_SHIFT_X` (6px) x-offset on top of the existing centre alignment - centering the now-
shorter "100" instead of "100%" in the same box already shifts it right by about half a character,
this adds one more half on top per the user's explicit "then move it another half character over".
Date: moved down 4px (label y 29->33, shadow 30->34) and given a 1.15x (`transform_scale` 294,
LV_SCALE_NONE=256=100%) enlargement on both date_shadow/date_label per "the date number could go
down a bit more and made a little bigger" - grew `DATE_BOX_H` 54->58 for headroom (real panel allows
up to y271, box bottom now at 261, still inside it) to give the enlarged text room without crowding
the box edge. Note the DSEG font swap changes the *day* label's look too even though only date/
battery were asked to be resized - accepted as expected fallout of a global font swap, not adjusted
further since only date/battery had explicit sizing asks.

Confirmed shadow coverage (task from earlier this session) needs no code change: day_shadow,
date_shadow, and batt_shadow already existed for all three LCD text elements (offset dark copy
underneath, same emboss technique) before this pass, and there are no other `lv_label_create` calls
anywhere else in this face - the LCD-window text is the only text this face draws itself.

Built clean (RAM 45.3%, flash 95.8%, both fit), flashed via `pio run -t upload`, boot log clean
(RTS-toggle reset read, ESP-ROM banner through "Starting up device", no panic/backtrace). Not yet
visually confirmed on the actual device - user is checking now. This closes out the full batch of
2026-07-17 feedback (hand pivots/colors/lengths, LCD layout/font) in one session; next step is
on-device confirmation and likely a fresh round of fine-tuning from what the user sees.


2026-07-18 (cont.) — citizen_410: DSEG14 font crashed the board, reverted to Montserrat
------------------------------------------------------------------------------------------------
The LCD font swap above did NOT survive contact with hardware - user reported the watch stuck in a
boot/crash loop shortly after flashing. Serial log (captured via the usual RTS-toggle reset trick,
this time read for longer than the usual quick glance) showed the real cause: boots fine, prints
the v6.3/MAC banner, then `Guru Meditation Error: Core 1 panic'ed (LoadProhibited)` with
`EXCVADDR: 0x00000010`, in `draw_letter_cb` (lv_draw_sw_letter.c:176) via the label draw path - a
NULL-pointer-plus-small-offset dereference while blending a glyph bitmap (`draw_buf->data` where
`draw_buf` = `lv_font_get_glyph_bitmap(...)` returned NULL). Decoded with
`xtensa-esp32s3-elf-addr2line -pfiaC` against `firmware.elf` from the crashing build (kept the
`.pio` build dir around specifically because it still matched the flashed binary - if that gets
cleaned before diagnosing a crash, the addresses in a serial dump become useless).

Root cause: `dseg14_bold_18` - this was the DSEG14 LCD-style font's actual *first-ever* use in this
face (generated a while back, declared, but never wired into a live label until this session's font
swap for day/date/battery). Something in that font's converted data isn't compatible with this
project's LVGL 9.3 build - skimmed the glyph_dsc table for the obvious things (glyph ids, cmap
ranges, bitmap_index math) and nothing jumped out as blatantly corrupt, glyph coverage looked right
for the day-name/digit text actually being rendered, so didn't fully pin down *which* glyph or *why*
`lv_font_get_glyph_bitmap` came back NULL - didn't chase it further once the fix was obvious.

**Fix: reverted DATE_FONT/BATT_FONT back to Montserrat** - `lv_font_montserrat_22` for the date (a
built-in, known-good font, and bumping 20->22 satisfies the original "make it a little bigger" ask
without needing DSEG at all), `lv_font_montserrat_20` for battery (unchanged size, no bigger ask
there). Dropped the `transform_scale` 1.15x hack on the date labels too since it was only there to
compensate for DSEG's fixed 18px - not needed once the font itself is a normal built-in size. Kept
`DATE_BOX_H` at 58 (headroom for montserrat_22's slightly taller line) and the battery %-removal +
`BATT_EXTRA_SHIFT_X` position changes (unrelated to the crash, those are just label text/position,
not font machinery). Rebuilt, reflashed, watched the serial log for a full 10s past the point where
it used to panic this time instead of the usual quick glance - held clean, no crash-loop.

**DSEG14 font is parked, not fixed** - if the LCD-digital look is still wanted, it needs real
investigation (possibly regenerating it with a different lv_font_conv version/target, or bisecting
which specific glyph triggers the NULL return) before it goes anywhere near a live label again. Add
a scratch/throwaway label somewhere non-critical to test a font change in isolation before wiring it
into the main UI, rather than swapping in three labels at once next time - would have made this
failure much cheaper to isolate.


2026-07-18 (cont. 2) — citizen_410: hand shrink, divider removed, shadow made prominent
------------------------------------------------------------------------------------------------
Further feedback after the crash-and-revert: minute/second hand tips still too long (should sit
inside the inner bezel tick marks, not reach/touch them - the 140px target from earlier this
session was apparently still too generous once seen on real hardware), no divider line wanted under
the day/date number, and the day/date/battery shadow effect turned out to be so subtle the user
hadn't even noticed it was there - asked for a real, prominent, clearly-offset "LCD text shadow"
look instead, disconnected from the main text rather than a near-1px emboss.

**Hands re-scaled, not re-extracted.** Since the minute/second sprites built earlier this session
already have correct pivots (on the real screw-holes), correct color, and correct connectivity, a
uniform image re-scale around the existing pivot is enough - no need to redo the photo-forensics.
Scaled both from 140px pivot-to-tip down to 128px (factor 0.9143), pivot recomputed as
old_pivot*factor (minute (31.06,75.91)->(28.4,69.4), second (70.2,54.2)->(64.2,49.6)) - this keeps
the pivot at the *same relative position on the hand's geometry* (satisfies "keep pivot points on
same place on hands") while shrinking the whole sprite uniformly. Base angles unchanged (a uniform
scale doesn't rotate anything). Sanity-checked with one composite render at 10:10:30 before
flashing - both hands land well short of the inner tick ring now. 128px was a judgement call, not a
precise measurement (no way to read the exact tick radius off the real screen from here) - flagged
to the user as a first cut, may need another round.

**date_divider removed entirely** - deleted the `lv_obj_create` for it and its static var; no
replacement, just gone.

**Shadow made properly prominent on all three labels** (day, date, battery) - previously the shadow
copy was offset by only ~1px from the main text (day_shadow (1,2) vs day_label (0,1) - barely
distinguishable, which is exactly why the user hadn't noticed it existed). Pushed the shadow copies
out to a clearly-separated (+4,+4)-ish offset from each main label instead. This meant growing the
box heights for headroom: `DATE_BOX_H` 58->62, `BATT_BOX_H` 25->27 (both still comfortably inside
the true LCD panel bounds measured earlier this session) since the shadow copy now sits visibly
lower than the label instead of nearly on top of it.

Built clean (flash 95.8%, unchanged), flashed via `pio run -t upload`. This time watched the serial
log for the full 10s past the point where the DSEG crash used to hit, not just a quick glance -
clean boot, `AXP2101 battery: 6%, charging: 1` (on charger, different state than earlier reads -
fine, unrelated). Not yet visually confirmed on-device - user is checking now, plus still owes a
closer look at the hand pivots from the previous round ("will check more later").

User also asked whether a separate isolated test was being suggested for the DSEG14 font (yes -
see the "DSEG14 font is parked, not fixed" note above) - confirmed that's the recommended next step
if the LCD-digit look still matters, not started yet, no urgency.


2026-07-18 (cont. 3) — citizen_410: shadow tuned down, hour hand junk fixed, minute hand red removed
------------------------------------------------------------------------------------------------
More feedback after the previous round: hand sizes now "looking better" (no change requested), but
the new shadow was too far/too dark ("coming in a bit and made lighter"), the hour hand had visible
junk around its outside edge, the minute hand still showed red, and the user asked to see the real
device at 09:50 specifically to check the right-hand (date) LCD window with the hands clear of it,
plus said no lines wanted above/below the LCD characters (noted but not diagnosed - no border/
underline styling found anywhere in the label code, nothing obvious to fix blind; asked user to
point at it directly once they can see the 09:50 render, cheaper than guessing further).

**Shadow dialed back**: offset (+4,+4) -> (+2,+2) relative to each main label on all three (day,
date, battery), color 0x3a3a3a -> 0x5a5a5a (lighter grey).

**Hour hand junk root-caused and fixed**: the darkish-pixel-inclusion step from the 2026-07-18
rebuild (added to recover the black outline near the kept white arrowhead fragments) dilated far
enough right to bridge into the neighbouring 24h sub-dial's tick marks, pulling in their alternating
light/dark pattern as a zigzag noise blob along the hand's right edge. Fixed by capping that
inclusion to x<415 in source-photo space (past the real arrowhead's right edge, short of the tick
marks) - tip/pivot came out numerically identical to before (94,61), confirming the junk was pure
disconnected debris, not real hand geometry.

**Minute hand red removed entirely**: re-examined why "still red" after the counterweight-restore
fix - the red hook sat right where the pivot is, matching the exact spot the 2026-07-17 GIMP cleanup
was targeting (that cleanup existed specifically to remove second-hand chronograph red bleed off
this same hand). Safer to treat as leftover bleed than a genuine design element rather than keep
guessing - recolored all red-ish pixels (loose threshold to catch anti-aliased fringe, not just
solid red) to near-black. Hand now reads plain black/silver, matching the hour hand.

**Time temporarily forced to 09:50:00 (2026-07-18)** in `hal/esp32/app_hal.cpp`'s RTC init, right
after the normal RTC->watch time read, clearly marked `TEMPORARY` in a comment - so the user could
check the date LCD window with the hands out of the way. **This runs on every boot until removed** -
must be deleted (or the RTC re-synced) once the check is done, otherwise the watch will keep
resetting to this fixed time on every reboot instead of keeping real time.

Built clean, flashed via `pio run -t upload`, watched serial for the full 10s again - clean boot,
`AXP2101 battery: 23%, charging: 1`. Not yet visually confirmed on-device - user is checking now,
including the date LCD window at 09:50 and the "lines above/below characters" concern.


2026-07-18 (cont. 4) — citizen_410: watch body/case added behind the dial
------------------------------------------------------------------------------------------------
New idea from the user: add the watch's own body (crown, pushers, bracelet) back in behind the
dial, which had been a tight circular crop on plain black since the face was first built. Scoped
carefully through discussion first: scale must match the *existing* dial exactly (no moving/
rescaling the dial itself - it's the anchor), clip everything outside the 410x494 draw area rather
than trying to fit the whole watch in, and crown loss was explicitly accepted as fine.

**Pattern-matched instead of eyeballed.** User specifically asked for this rather than a manual
grid-overlay crop (the method that produced the "crown/pusher bias" problem logged earlier this
project for a different photo). Used OpenCV: decoded the *existing* face_citizen_410_dial_img back
to a PNG, Canny-edge'd both it and the full-body source photo
(`CitizenE-DriveRedArrowsRAFA_900x.webp` - confirmed this is where the dial content originally
traces back to, just never cropped for anything beyond the dial itself), then did a multi-scale +
multi-rotation `cv2.matchTemplate` search (scale ~0.65-1.05 coarse, refined to 0.76-0.86 fine, angle
+-3deg) to find the transform that makes the *existing* dial align with its own appearance inside
the full photo. Best match: scale 0.804, rotation 0.0deg. Verified with a 50/50 pixel blend overlay
before trusting it - bezel scale numbers and tick marks lined up cleanly, only minor softness, no
visible doubling.

**First attempt only showed the top/bottom letterbox** (crown correctly absent - confirmed clipped
outside the 410px width at this scale, exactly as expected given zero horizontal margin was
available; pushers partially visible at the sides). User pointed out the four triangular corners of
the *dial's own 410x410 square* (outside its inscribed circle, inside the square canvas) were still
solid black - initially misread this as "no body content there" (checked the wrong region, the
414x494 screen's outer letterbox, and confirmed via zoomed crops that region genuinely is white
studio background) before realising the actual ask was about the *dial asset's own* corner masking:
`face_citizen_410_dial_img` was originally encoded as opaque RGB565 with those 4 corners painted
solid black (correct/harmless when the canvas behind it was *always* black - see the original
"real alpha would be redundant" note from when this face was first built) - now that there's real
body content behind it, that opaque black hides it.

**Fix: re-encoded the dial as RGB565+alpha**, corners set fully transparent, circle content
untouched. Circle boundary re-measured directly off the existing asset (360-sample radial scan
from centre (205,205): median radius 205.0, matching the canvas half-width as expected) with a
1.5px feathered edge to avoid a hard seam. New `face_citizen_410_body_bg` asset (410x494, opaque,
no alpha needed since it's the base layer) drawn full-screen *behind* the dial. Body photo's own
white background flood-filled to black from the image borders (not a blanket brightness threshold -
anchored at the four edges specifically so it doesn't eat into bright/reflective metal that's
actually on the watch).

**Flash budget didn't fit at first** - alpha dial (504300B) + new body bg (405080B) needed ~570KB
more than the ~321KB free (was already at 95.8%). User chose to disable two of the other enabled
`_410` faces to make room rather than compress harder: `ENABLE_FACE_228_410` and
`ENABLE_FACE_1167_410` in `hal/esp32/app_hal.h` (commented out, not deleted, easy to re-enable).
Freed ~1.6MB - well over what was needed, landed at 84.8% after adding the new assets back in
(was 95.8% before any of this, so net *more* headroom than the session started with).

Built clean, flashed via `pio run -t upload`, boot log watched for the full 10s again - clean,
`AXP2101 battery: 100%`. **User confirmed on-device: "Very good."** - body/case background, the
transparent-corner fix, and the pattern-matched alignment all look right on the real screen. More
work planned but not yet specified - see the "pinned" open-questions section earlier in this file
(lines above/below LCD characters, hand pivot/length further checks, DSEG14 font, and the temporary
09:50:00 time override in `hal/esp32/app_hal.cpp` which is *still in place* and still needs removing
or the RTC re-syncing).


2026-07-18 (cont. 3) — face swap (174_410 -> 1167_410) + 2151_410 weather-temp bug fixed
------------------------------------------------------------------------------------------------
User asked to reinstate one of the two faces dropped earlier this session for citizen_410's flash
budget (see the "Flash budget didn't fit at first" entry above), giving up a different currently-
enabled face instead of re-cutting the budget. Faces identified visually (sent previews via
`watchface.png` per face dir, not by macro name/number - numbers meant nothing to the user):
**1167_410** (cyan tech face, weather+battery+activity rings) reinstated, **174_410** (plain BPM/
steps/kcal digital face) disabled to make room. `228_410` (Santa/moon) stays off, untouched.
`hal/esp32/app_hal.h` ESPS3_2_06 block updated accordingly. Built clean: **flash 83.8%** (well
under budget - 1167_410 is smaller than the citizen_410 additions that prompted the original cut).
Flashed, boot-log clean, BLE reconnected same-session. **User confirmed the right faces are
disabled** afterward.

Side finding, not acted on: `ENABLE_FACE_1041_410` in `app_hal.h` is a phantom - defined but with
no corresponding `src/faces/1041_410/` implementation and zero other references anywhere in the
codebase. It compiles fine (just an unused macro) but produces no actual watchface on-device. Not
in scope to fix/remove now, just flagging it exists.

**2151_410 ("blood moon") weather temperature bug - same class of bug as the red_magic_410 fix
above: static placeholder digits, never wired to real data.** `update_weather_2151_410(temp, icon)`
only ever updated the weather icon; the `temp` parameter was silently discarded, and the three
temp-digit objects (`face_2151_410_7/8/9_207436`) sat permanently on their initial static glyph.
The digit-glyph strip asset needed for this already existed
(`face_2151_410_dial_img_7_207436_group[0..9]`, confirmed by decoding index 0 with
`tools/facegen/lvgl_img.py` - a clean "0" glyph), it just wasn't being indexed anywhere.

Confirmed the digit place-value convention (higher x = less significant digit) **within this same
face's own already-working code** (steps: x=152/133/114 for ones/tens/hundreds; battery: x=123/
105/88) rather than assuming it carries over from red_magic - it does, cross-checked and consistent.
Applied: `face_2151_410_7_207436` (x=225, ones), `_8_207436` (x=206, tens), `_9_207436` (x=187,
hundreds, hidden below 100) now indexed by `(t/1)%10` / `(t/10)%10` / `(t/100)%10` off a clamped
`t = temp < 0 ? 0 : temp` - no genuine minus-sign glyph found for this face (see below), so negative
temps clamp to 0, same accepted-limitation precedent as the working `1167` face.

Also decoded items `10_212058` and `11_213871` (both sit at the same x=242,y=8 position, previously
both drawn with neither hidden - identical bug shape to red_magic's °C/°F overlap) - confirmed
`10_212058` = "°C", `11_213871` = "°F" by direct visual decode (no metadata anywhere for this).
Now explicitly shows °C, hides °F.

Built + flashed, boot-log clean, BLE fine. **User confirmed digits now show real data** but reported
a **speckled minus-sign artifact** - same shape of bug as red_magic's "speckle left of the tens
digit" (see the 07-16 entries above): items `5_65535`/`6_65535`/`12_65535` all sit directly under/
between the digit positions (x=225/206/193, same y=25 row) sharing one placeholder image asset
(offset `65535` = no real image data per `items.txt`). Decoded that placeholder anyway out of
caution - it's a small, incoherent 13x5 speckle, not a clean dash shape, reinforcing these aren't a
real functional minus-sign mechanism for this face (unlike red_magic's genuine dash-per-digit
design). Applied the same belt-and-suspenders fix as red_magic's speckle bug: moved all three
objects physically off-canvas (`x = -100`) in addition to `LV_OBJ_FLAG_HIDDEN`, at their creation
site (not per-update, since they're never meant to be shown). Built, flashed, boot-log clean.
**User confirmed: speckle gone.**

All three changes (face swap, temp-digit wiring, speckle fix) flashed and confirmed on-device this
session. Not yet committed to git along with the rest of this session's uncommitted citizen_410/
red_magic_410/classic_410 work.


2026-07-20 — citizen_410: session committed+pushed, then black lines above/below LCD windows fixed
------------------------------------------------------------------------------------------------
Picked this project back up in a new session. First housekeeping: the entire multi-day uncommitted
pile (citizen_410, red_magic/red_magic_410, classic_410, the 1167_410/174_410 face swap, the
2151_410 weather fix) was batch-committed to `gremlinDev` (`110ef05`) and pushed to the `origin`
fork. Push initially failed with GitHub's "password authentication not supported" - fixed via
`gh auth setup-git` (wires the already-logged-in `gh` token in as git's credential helper); see
memory `reference_gh_github_auth` for next time. Also confirmed the previously-flagged "temporary
09:50:00 time override" is no longer present in `app_hal.cpp` - already resolved, notes were stale.

**Then the user reported (from a photo, description-only this session - no image was actually
re-attached) unwanted black lines above and below the date window**, guessing it might be related
to how the LCD-panel background color was matched. Investigated by decoding
`face_citizen_410_lcd_bg`/`_lcd_bg_left` back to pixels (`tools/facegen/lvgl_img.py`) and measuring
per-row brightness rather than guessing from the C code alone - confirmed **two real, compounding
bugs**:
1. **Heavy Gaussian-blur edge-darkening baked directly into the patch pixels.** Top rows of both
   patches (and the bottom rows of the battery patch) were 40-50 brightness points darker than the
   plateaued interior - e.g. date patch row 0 was 94.8 vs a ~164 plateau; battery patch row 0 was
   84.6 vs a ~161 plateau. Classic blur-near-a-crop-boundary artifact, same family of bug as the
   already-logged "margin=10 pulled in red" battery-patch issue from 07-17, just not fully purged.
2. **Box/asset height mismatch, independent of the above.** `DATE_BOX_H` had grown to 62 at some
   point after the LCD background asset was last generated at 53x54 (its own header comment still
   said "grown to 53x54", stale) - an 8px gap. Same shape for the battery box: `BATT_BOX_H` 27 vs
   asset height 25, a 2px gap. Non-tiled `bg_image_src` on an `lv_obj` doesn't stretch to fill a
   larger box by default (`scale_x`/`scale_y` default to `LV_SCALE_NONE` - checked
   `lv_draw_image_normal_helper` in the vendored LVGL source directly rather than assume), so these
   extra rows at the bottom of each box were left completely undrawn.

**Fix**: for each patch, measured per-row brightness, trimmed off any row below 85% of the
plateau's brightness (removes the dark edge bands), then resized the remaining clean core
(LANCZOS) to exactly the real box dimensions (53x62, 54x27) - this fixes both bugs in one pass,
since it removes the darkening *and* closes the size gap. No original source photo needed - worked
directly off the already-baked-in asset pixels via `tools/facegen/lvgl_img.py`'s
decode/encode/format helpers. Verified after: date patch row 0 brightness 94.8 -> 139.4, battery
patch row 0 84.6 -> 142.7, both now close to their plateau instead of near-black.

Built clean (flash 84.0%, comfortably under budget, `.pio` output unchanged in shape from before),
flashed via `pio run -t upload`, boot log watched via a DTR/RTS reset pulse - clean, `AXP2101
battery: 100%, charging: 0`, `Setup done`, no crash-loop. Committed (`85953c1`) and pushed.
**User confirmed the fix on-device.** Also asked for the day/date divider bumped 1px -> 2px
(`day_date_divider` height in `citizen_410.c`) - flashed, confirmed good, committed (`9fd9a5d`).


2026-07-20 (cont.) — citizen_410: DSEG14 crash root-caused and fixed, real LCD font now live
------------------------------------------------------------------------------------------------
Root cause of the crash-loop parked since 07-18 finally found via source-level analysis, not
guesswork: `lv_font_conv` defaults to RLE-compressed glyph output (`bitmap_format=1` in the
generated font_dsc), but `include/lv_conf.h:429` has `LV_USE_FONT_COMPRESSED 0`. Checked
`.pio/libdeps/lolin_s3_2_06/lvgl/src/font/lv_font_fmt_txt.c` directly: when compressed but not
supported, `lv_font_get_bitmap_fmt_txt()` logs a warning and returns `NULL` - exactly the null
`draw_buf->data` deref crash decoded back on 07-18 (`lv_draw_sw_letter.c:176`). Fixed by
regenerating `dseg14_bold_18.c` with `lv_font_conv --no-compress` (same TTF, same glyph ranges,
`fonts-DSEG_v046/DSEG14-Classic/DSEG14Classic-Bold.ttf` still present in an old scratchpad) -
`bitmap_format=0` (PLAIN) afterward, sidesteps the disabled code path entirely rather than
touching the global `lv_conf.h` flag (safer - avoids affecting every other font in the project).

**Isolated test first, per the standing plan**: added a throwaway `dseg14_test` label on top of
the face showing "01:23", built, flashed, watched the boot log - clean, no crash. Confirmed the
font itself now works before touching anything live.

**Wired into the real day/date/battery labels**, removed the test label. `DATE_FONT`/`BATT_FONT`
switched from `lv_font_montserrat_22`/`_20` to `&dseg14_bold_18`. Built+flashed clean (boot log
watched via DTR/RTS reset), no crash. **User confirmed it looks good on-device.**

Two follow-up tweaks, both user-requested after seeing it: **battery right-justified** - replaced
the old `LV_TEXT_ALIGN_CENTER` + manual `BATT_EXTRA_SHIFT_X` hack (a leftover from when the
trailing "%" was dropped) with real `LV_TEXT_ALIGN_RIGHT` + a small `BATT_RIGHT_PAD` (3px) inset
from the box's rounded corner. **Date (day-of-month) digits nudged down** ~1/4 character
(DSEG14-18's `line_height`=18, so 5px) - `date_label`/`date_shadow` y positions 32/34 -> 37/39,
kept the shadow's existing +2/+2 offset convention intact. Built, flashed, boot-clean, user
confirmed both. Committed together (`fe98670`) since the font fix and the two tweaks were one
continuous flash-and-check loop.


2026-07-20 (cont. 2) — citizen_410: dial nudged 3px left on-screen, body/case re-fitted to match
------------------------------------------------------------------------------------------------
User flagged the dial reading slightly right-of-center (noticed looking at the case area outside
the round dial) and asked for a 3px correction. Important scope clarification mid-discussion after
an initial back-and-forth: **the dial's own picture must not be regenerated/re-cropped** - only
its on-screen draw position, plus hands and LCD text, should move. The **body/case picture** is
the one that gets regenerated/re-fitted to match wherever the dial ends up.

Implementation: new `DIAL_SHIFT_X` (-3) constant in `citizen_410.c`, applied to `dial_img`'s
`lv_obj_set_pos` (raster itself untouched), `SCREEN_CX` (shifts all hand pivots + the center cap
in one place), `DATE_BOX_X`, `BATT_BOX_X`. `body_bg` stays drawn at (0,0) - its shift is baked
into the regenerated picture instead, so the two mechanisms combine into one uniform 3px-left
shift of the whole assembly on the physical screen while dial-vs-body relative alignment is
preserved (verified this reasoning with a written-out coordinate trace before touching code, not
just intuition).

**Re-deriving the body/case crop without trusting old notes.** The original 07-18 session's exact
webp crop rectangle (center/scale in `CitizenE-DriveRedArrowsRAFA_900x.webp`) was never recorded
numerically, only "scale 0.804, rotation 0.0". Rather than re-run the fragile from-scratch
Canny-edge dial-vs-photo matchTemplate search (tried first, got weak/boundary-hugging 0.06-0.25
confidence results - masking the dial's artificial black corners out of the correlation with a
proper `matchTemplate` mask helped some (0.83) but still landed on an implausible scale/location
when checked visually), found a much more reliable path: a leftover verification image from that
original session, `match_compare.png` (side-by-side dial vs. matched-crop, still in an old
scratchpad), gave a clean, already-correct 410x410 resized reference crop. Matching *that* back
against the same webp photo (rather than the dial itself) landed on 0.976 confidence, crop origin
(196,149), size 510x510 (scale 410/510=0.8039, rotation ~0) - matches the documented "0.804, 0deg"
almost exactly, confirming the recovered rectangle is trustworthy. Verified further with a 50/50
blend overlay against the existing `body_bg` asset before trusting it (bezel numbers, bracelet
links, all sharp, no doubling).

Extended that rectangle vertically for the full 410x494 screen (same recipe as the original: X
unchanged, Y extended by `Y_OFFSET` each side, converted through the 0.8039 scale), shifted the
X-origin by `3/0.8039 =~3.73` webp px (right, since moving the crop window right shifts the
*output* content left), re-resampled fresh from the real photo - the newly-exposed edge strip is
genuine photo content, not synthesized.

**Flood-fill bug found and fixed before flashing.** First attempt at re-flood-filling the photo's
white studio background to black (same technique as 07-18) seeded from *every* border pixel
without checking color first. The bracelet touches the top/bottom crop edges with its own dark
metal tones - those became bad seed origins, and flood-filling outward from a dark grey seed (even
with `FLOODFILL_FIXED_RANGE`) ate the connected crown/pusher/bracelet detail, nearly erasing both
pushers and most of the crown. Caught by a side-by-side zoom comparison against the existing asset
before flashing - user's own description ("harsh", "taking detail from left and right") matched
exactly what the comparison showed. Fixed by only seeding from border pixels that are genuinely
near-white (`min channel > 220`) - 374 of ~410 candidate seed points were real watch content and
correctly skipped, only 12 true-white seeds used. Crown/pusher detail confirmed back, arguably
slightly more visible than the original asset (this crop extends ~3.7px further right).

Built, flashed, boot-log clean both times (first with the flood-fill bug, then the fix - user
caught the bug from a sent image before it was even flashed the second time... actually was
flashed once with the bug for the shift-only check, then again with the fix). **User confirmed
good result on-device.** Committed (`de47ec7`) and pushed.


2026-07-20 (cont. 3) — GitHub release published for fbiego
------------------------------------------------------------------------------------------------
User wants to show this work to the upstream author (fbiego). Tagged current `gremlinDev` HEAD
(`de47ec7`) as `citizen-410-preview-2026-07-20`, pushed the tag, and created a GitHub release on
the fork (`BigGremlin/esp32-c3-mini`) via `gh release create` - title "Citizen Eco-Drive watchface
(WIP preview)", release notes summarizing citizen_410/red_magic/red_magic_410/classic_410, with
the freshly-rebuilt `lolin_s3_2_06_v6.3_0x0.bin` attached as a flashable asset. Release:
https://github.com/BigGremlin/esp32-c3-mini/releases/tag/citizen-410-preview-2026-07-20

Still-open items, unchanged: hand pivot/length further review ("will check more later", never
followed up), and the "lines above/below LCD characters" concern from 07-18 - worth checking
whether that's actually resolved now given the LCD background rewrite two sessions ago, or still
needs a dedicated look.


2026-07-20 (cont. 4) — Extreme Power Save: investigation + plan, steps 1-2 done
------------------------------------------------------------------------------------------------
New feature request: port as much of OLEDS3Watch-joaquim's power-management work as makes sense,
gated entirely behind a new "Extreme Power Save" Settings switch - when on and on battery, sleep
as deep as possible (CPU, touch, BLE all off); when off, current behaviour unchanged. Before
touching code, did two rounds of investigation per explicit user request.

**Button investigation.** This board (`ESPS3_2_06`) has two physical buttons: GPIO0 ("BOOT") and
a bare hardware EN/CHIP_PU reset (never software-visible on any ESP32 board, nothing to reference
in firmware, identical behaviour regardless of project). `BUTTON_HOME` (GPIO0) is an established
pattern already used by `ELECROW_S3`/`VIEWE_*`/`ESP32_CYD` in `pins.h`, and confirmed as real,
working hardware on this exact board via `OLEDS3Watch-joaquim` (same physical board, GPIO0 polled
every 50ms for its button-sleep feature) - but `ESPS3_2_06` never had `BUTTON_HOME` defined, so
chronos-watch's existing `Button2`/`btn_home_handler` code (single/double/triple/long-click, all
already written) was simply dead for our board. Zero risk of clobbering anything on our board.

**Display-pipeline risk investigation**, since OLEDS3Watch-joaquim's own attempt at real light
sleep (`8a4fa1b`/`81340bd`) caused a confirmed, severe display-banding regression there and was
fully reverted (`58708c9`) - root cause never fixed, just avoided. Checked joaquim's *current*
state precisely rather than trusting old notes: `CONFIG_FREERTOS_USE_TICKLESS_IDLE is not set`,
`display_manager_pm_early_init()` defined but never called - light sleep is genuinely inert there
today. Joaquim's own root cause was `trans_queue_depth=32` (deep async SPI transaction queue)
getting transactions dropped/reordered under load. Chronos-watch's own display stack was checked
directly rather than assumed safe-or-not: same general chunked-partial-buffer LVGL pattern (40-line
buffer, even shorter than joaquim's 100), but a **completely different driver** - `Arduino_GFX`
(not ESP-IDF's `esp_lcd` BSP driver), `Arduino_ESP32QSPI` bus with `.queue_size = 1` (checked the
actual library source) - i.e. transactions are effectively blocking/synchronous, one at a time.
This structurally rules out joaquim's specific reorder/drop failure mode (there's nothing to
reorder with a depth-1 queue), though a lesser sleep-induced-delay-between-chunks risk could still
exist and needs real on-device testing, not just this static analysis.

**Also confirmed**: chronos-watch has zero WiFi usage anywhere (nothing to turn off there), and
ChronosESP32 already exposes a clean `watch.begin()`/`watch.stop(bool clearAll)`/`watch.isRunning()`
API for BLE - `stop(true)` fully deinits NimBLE, no internal hacking needed. `TouchDrvFT6X36` (our
touch driver) already has `sleep()`/`wakeup()` methods (wakeup calls `reset()` internally) - cleaner
than joaquim's manual GPIO-reset-hold. User confirmed: BLE should be **fully stopped** (not just
touch/CPU sleeping) while asleep on battery - it's a real power draw and phone reconnect-on-wake is
an acceptable tradeoff.

**Agreed plan, staged, one tested/flashed piece at a time** (matching this project's established
pattern): (1) enable GPIO0 button, isolated test. (2) Add the Settings switch, persisted, no
behaviour yet. (3) Charge-aware screen timeout (stay on indefinitely on `PMU.isVbusIn()`, button
forces off regardless of power state) gated by the switch. (4) Touch sleep/wake tied to
screen-off-while-on-battery + switch. (5) BLE stop/begin tied to the same condition - flagged to
verify on-device whether the phone reconnects cleanly after a stop/begin cycle. (6) CPU light sleep
last, its own isolated experiment, since it's the one piece with no working precedent to lean on.

**Step 1 DONE, tested, committed (`a8d692f`).** Defined `BUTTON_HOME 0` for `ESPS3_2_06` in
`pins.h` - no other code changes needed, the existing Button2 wiring just activated. Built,
flashed, boot-clean. **User confirmed**: top button is GPIO0, single-click/double-click/long-press
(home/app-list/face-picker) all working as expected.

**Step 2 DONE, tested, committed (`56abfea`).** New `ui_extremePowerSave{Panel,Icon,Switch,Label}`
row in Settings (`src/ui/ui.c`), placed right after the Screen Timeout row, built identically to
the existing "Auto Start Nav" switch (icon + `lv_switch` + label, `ui_img_battery_state_png` icon).
`onExtremePowerSave()` in `app_hal.cpp` sets a new `extremePowerSave` bool and persists it via
`prefs.putBool("extremepwr", ...)`, loaded at boot the same way `navSwitch`/`autonav` already is.
Added the `onExtremePowerSave` prototype to `ui_events.h` plus no-op stubs in the pico/sdl2 HAL
backends (matching `onNavState`'s existing per-backend pattern) so all three build targets stay
consistent - only the esp32 backend has a real implementation. **No sleep/BLE/touch behaviour
wired up yet, deliberately** - this step is purely "does the toggle persist."
Built, flashed, boot-clean. **User confirmed**: toggle persists across reboot. Also noted the
switch doesn't show its blue checked-state fill after a reboot even though the state is genuinely
set - checked against the existing "Auto Start Nav" switch, which does the exact same thing, so
this is a pre-existing app-wide LVGL/theme cosmetic quirk, not a regression from this change. Not
fixed, out of scope, user said to forget about it.

**Step 3 DONE, tested, committed (`5b7bf59`).** New `staying_on_for_charging()` helper (returns
`extremePowerSave && PMU.isVbusIn()` on `ESPS3_2_06`, hardcoded `false` on every other board so
their behavior is byte-for-byte unchanged - other boards have no AXP2101/VBUS sensing at all).
Main-loop screen-timeout expiry check gets an extra `!staying_on_for_charging()` guard - screen
just never auto-times-out while it's true.
**Real bug caught before flashing, not after**: the GPIO0 button's existing single-click
force-off used a "rewind `screenTimer.time` into the past, let the normal expiry check fire on the
next tick" trick - which stops working the moment that same expiry check is suppressed for
charging. Traced through the logic by hand before testing on-device and fixed it: when
`staying_on_for_charging()` is true, the button now clears `screenTimer.active`/brightness
directly instead of relying on the rewind trick. The original (on-battery, or switch off) button
path is completely untouched - only the new charging-aware case got the direct branch.
Built, flashed, boot-clean. **User confirmed all three behaviors on-device**: screen stays on
past a short timeout while plugged in with the switch on; the button turns it off on demand in
that state; unplugging restores the exact original timeout behavior.

**Step 4 DONE, tested, committed (`e94f25f`).** New `touchAsleep` flag. `tft.touch.sleep()` called
right alongside the existing screen-off action in the main-loop timeout branch, gated by
`extremePowerSave && !PMU.isVbusIn()` (same condition family as step 3). Two things caught by
reading the touch driver source before writing any of this, not by trial and error on-device:
1. `TouchDrvFT6X36::wakeup()` calls `reset()`, which is a real ~200ms blocking GPIO reset sequence
   (10ms + 30ms + 160ms delays) - NOT cheap. `screen_on()` is called from `my_touchpad_read()` on
   every ordinary touch press, so unconditionally waking touch there would add a 200ms stall to
   every single tap. Fixed by only calling `wakeup()` when `touchAsleep` is actually true - it can
   only be true via the button/alarm wake paths, since a sleeping touch chip can't generate a press
   to reach the touch-driven call site in the first place.
2. `my_touchpad_read()` was still polling `tft.getTouch()` (I2C) every ~5ms tick even while the
   chip is asleep, for nothing - a sleeping FT6X36 can't wake itself, only the button can. Added an
   early return (report not-touched, skip the I2C read entirely) while `touchAsleep` is true.
All new code `#if ESPS3_2_06`-gated, zero effect on other boards. Built, flashed, boot-clean.
**User confirmed on-device**: touch genuinely stops responding once asleep (by design - only the
button can wake it in that state), button wakes the screen and touch resumes normally afterward.

**Step 5 DONE, tested, committed (`0811039`).** New `bleAsleep` flag, exact same pattern as
`touchAsleep`. `watch.stop(true)` called alongside `tft.touch.sleep()` in the same gated block;
`watch.begin()` called from `screen_on()` only when `bleAsleep` is true. Checked
`NimBLEDevice::deinit()` source before trusting `clearAll=true` wouldn't cause re-pairing pain:
it only frees the C++ wrapper objects (server/advertising/scan), never touches the bond/pairing
store, so the phone's existing pairing survives regardless of `clearAll`. `begin()` recreates
those wrapper objects from scratch each time, so `clearAll=true` (matching the API's own default)
is the correct choice, not `false`.
**Testing had one real mid-session mixup worth recording**: user's first report ("button
disconnected... power on and it took several seconds") was initially read as "tested while
plugged into USB," which would have meant a real bug (that code path never touches BLE). Turned
out to mean "powered the screen back on" - all testing was actually on battery throughout. Once
cleared up: pressing the button on battery correctly disconnects BLE too, because it funnels
through the *existing* rewind-timer force-off trick into the exact same code path as a natural
timeout - confirmed expected, not a bug. The "several seconds vs near-instant" reconnect
difference between the two on-battery tests is almost certainly normal BLE/phone-side scan
variance (both paths call the identical `watch.begin()`), not a code difference.
**User confirmed on-device across three scenarios**: natural timeout on battery (instant
disconnect, near-instant reconnect), button-forced sleep on battery (also disconnects, confirmed
expected), and button-forced screen-off while genuinely on USB power (BLE stays connected
throughout - confirmed that path correctly never touches it).

2026-07-20 (cont. 5) — Step 6 (CPU light sleep) ATTEMPTED, ABANDONED - real USB-disruption bug found
------------------------------------------------------------------------------------------------
Investigated and implemented, then fully reverted (never committed - `git checkout` cleanly
restored `hal/esp32/app_hal.cpp` to the step-5 state, `0b92022`). Recording the full investigation
here so nobody re-attempts this blind later - the finding is real and toolchain-specific, not a
one-off fluke.

**First: confirmed the "automatic" path is a dead end, then found a viable-looking alternative.**
Checked the actual precompiled sdkconfig for this exact board/toolchain
(`~/.platformio/packages/framework-arduinoespressif32/tools/sdk/esp32s3/qio_opi/include/sdkconfig.h`,
`qio_opi` being `lolin_s3_pro`'s `memory_type` per its board JSON) - neither
`CONFIG_FREERTOS_USE_TICKLESS_IDLE` nor `CONFIG_PM_ENABLE` are defined anywhere in it. So the
automatic/tickless-idle approach joaquim tried isn't just risky here, it's not even compiled in.
But a manual/explicit alternative looked viable: `esp_light_sleep_start()` +
`esp_sleep_enable_ext0_wakeup()` don't depend on tickless idle at all. **Verified this empirically,
not by assumption** - added a temporary probe function (never called, guarded by a
compiler-unprovable `volatile bool` so the linker couldn't dead-strip it), built, confirmed it
linked cleanly (~8.4KB of real object code pulled in), then removed the probe with zero trace
before writing the real implementation.

**Implementation**: new `deep_idle_loop()`, entered from the top of `hal_loop()` only once
`touchAsleep && bleAsleep` are both already true (steps 4/5's own gate) - deliberately *not* relying
on automatic idle-triggered sleep, entering a dedicated loop only once nothing is being
rendered/transmitted, specifically to avoid joaquim's mid-flush-interruption failure mode. Loop:
`esp_sleep_enable_ext0_wakeup(GPIO_NUM_0, 0)` (button) + a 2s periodic timer re-check, calling
`esp_light_sleep_start()` repeatedly; breaks out on an `ESP_SLEEP_WAKEUP_EXT0` cause and lets the
normal `hal_loop()`/`btn_home.loop()` flow take over from there (deliberately does *not* call
`screen_on()` itself, so Button2's own click-type classification stays undisturbed).

**Bug 1, found and fixed**: button couldn't wake it at all. Read `sleep_modes.c` directly (this
machine has a local `~/esp-idf` checkout) - `esp_sleep_enable_ext0_wakeup()` switches the pin to
RTC-IO function during sleep prep but never touches its pull resistors; Button2's digital-domain
`INPUT_PULLUP` doesn't apply once the pin is in RTC function, so it floats during actual sleep.
Fixed with `rtc_gpio_pullup_en()`/`rtc_gpio_pulldown_dis()` before `esp_sleep_enable_ext0_wakeup()`,
plus `rtc_gpio_deinit()` after the loop exits to restore plain digital mode for Button2's
subsequent polling. **Built, flashed, tested - still didn't wake from battery.**

**Bug 2, found via a live-serial diagnostic build, NOT fixed - real toolchain limitation.** Couldn't
watch the actual on-battery failure live (being connected to this machine's USB *is* VBUS power,
which prevents the sleep path from ever engaging at all - a real methodological catch-22). Worked
around it with a temporary `on_battery()` override (hardcoded `return true`, replacing all four
`PMU.isVbusIn()` call sites for this feature) so the exact same code path could run while still
connected for live serial visibility, plus added verbose per-iteration logging
(`esp_light_sleep_start()`'s return code, wakeup cause, live `digitalRead(0)`). This revealed the
real picture:
- `esp_light_sleep_start()` itself works fine - returns cleanly every ~2s, `rc=0`, no hang. This
  ruled out the "PM component not initialized, sleep call hangs" theory outright.
- Every single wake reported `cause=4` (`ESP_SLEEP_WAKEUP_TIMER`) - never `cause=2` (`EXT0`), i.e.
  button wakeup genuinely never registers no matter what.
- **While watching, the device vanished from the USB bus entirely** (`lsusb` showed nothing
  matching Espressif's vendor ID, not just a renamed serial port) - even though it was plugged in
  continuously the whole time. It did not come back on its own; needed a physical unplug/replug to
  recover, every single time, across several repeated occurrences while trying to reflash the
  revert afterward.

**Conclusion: `esp_light_sleep_start()` disrupts USB connectivity on this exact board/toolchain
combination badly enough to need a physical replug to recover, and separately never delivers a
genuine EXT0 wakeup.** This is very plausibly *why* Espressif shipped this Arduino core variant
with `CONFIG_PM_ENABLE`/tickless-idle disabled in the first place - not an oversight, a deliberate
choice given unresolved peripheral interactions like this. Matches what the user saw independently
on real battery testing (stuck asleep, only a fresh USB connection - not the button - brought it
back), which in hindsight was the same USB-disruption symptom, not a hang inside the sleep call.

**Fully reverted, nothing committed.** `git checkout -- hal/esp32/app_hal.cpp` cleanly restored the
step-5 state (`0b92022`) - the diagnostic/probe code left zero trace. Had to fight through several
rounds of the device dropping off USB mid-reflash (same underlying disruption, lingering from the
firmware that was still running during the revert-and-reflash attempts) before successfully getting
the known-good step-5 build back onto the device and confirming a clean boot + normal heap
(~19KB free, matching pre-step-6 readings, vs ~14KB while the sleep code was resident).

**Extreme Power Save feature considered DONE as of here, steps 1-5 only.** Screen dimming, touch
sleep, and BLE stop already cover the bulk of the real power draw; CPU stays awake, which is a
real but comparatively small remaining gap. Step 6 is not being re-attempted without a materially
different approach (e.g. a from-source Arduino-ESP32 core build with PM properly enabled and
tested - a much larger undertaking, not a simple project-level change) - don't re-attempt the
`esp_light_sleep_start()` approach on this toolchain again without reading this entry first.


2026-07-20 (cont. 6) — Step 6 RETRIED with ext1 (user supplied external research), same conclusion
------------------------------------------------------------------------------------------------
User dropped a research document into the scratchpad
(`ESP32-S3_AMOLED_2.06_LightSleep_DeepDive.md`) after the cont. 5 abandonment, citing a real,
tracked framework bug (`espressif/arduino-esp32#6581` - "ESP32-S3 native USB CDC stops working
after light sleep") that matches the USB-disruption symptom exactly, and recommending
`esp_sleep_enable_ext1_wakeup_io()`/`esp_sleep_enable_ext1_wakeup()` over the `ext0` API tried
before - ext0 and ext1 are genuinely different RTC hardware paths on S-series chips, not aliases,
and the doc cited a Waveshare-repo issue (#9) about exactly this class of GPIO/RTC-IO wakeup
mismatch on this board family. Worth a real retry given a concrete, specific reason ext0 might
have been the wrong choice, not just re-trying the same thing hoping for a different result.

**Re-implemented with ext1**, reading `ext1_wakeup_prepare()` in `sleep_modes.c` directly first
(not assuming it behaves like ext0's prepare path) - confirmed the same missing-pull-resistor
gotcha applies, plus ext1 additionally calls `rtcio_hal_hold_enable()` on this chip (latches the
pin through sleep), which ext0 never did - added `rtc_gpio_hold_dis()` after wake, on top of the
existing `rtc_gpio_pullup_en()`/`pulldown_dis()`/`rtc_gpio_deinit()` from the ext0 attempt.

**Went straight to a live-diagnostic build this time** (temporary `on_battery()` override forcing
the on-battery code path while still connected via USB, plus verbose per-iteration logging of
`esp_light_sleep_start()`'s return code and the wakeup cause) rather than testing blind first,
given round 1 burned two blind attempts before getting real visibility.

**Result, watched live: the button wake genuinely worked.** `cause=3` (`ESP_SLEEP_WAKEUP_EXT1`),
`gpio0_level=0` (pressed), "woke via button (ext1), exiting loop" - a real, clean wakeup, and
critically the USB connection stayed alive through the whole sequence this time (no repeat of the
cont. 5 USB-vanishing-from-the-bus symptom, at least while connected). Minor cosmetic quirk
observed while the button was held: rapid re-entry into `deep_idle_loop()` a few times in quick
succession (level-triggered wake re-fires instantly if the pin is still held low on re-entry) -
not investigated further, moot given what came next.

**Then the real test - genuinely on battery, USB fully disconnected - failed identically to before:
button did not wake it, needed a full reboot to recover.** This is the critical finding: the
successful diagnostic test was almost certainly an artifact of USB being physically attached, not
a real fix. The research doc itself flagged the mechanism: "ESP-IDF keeps the BBPLL powered while
a USB host is attached" (to avoid bricking the console on wake) - if that same clock domain is
what the RTC-GPIO wake circuitry actually depends on to function correctly, connecting USB for
diagnostics was inadvertently keeping the wake mechanism alive, and disconnecting it (the real
target condition) removes exactly what made the diagnostic test pass. This is not something fixable
in application code - it would be a board-support/SDK-level clock configuration question.

**Fully reverted again, nothing committed** (`git checkout -- hal/esp32/app_hal.cpp`, clean, same
as cont. 5). Confirmed working step-5 state restored (heap ~19KB free, matching pre-step-6
readings), boot clean.

**This closes step 6 for real, not just "one more idea to try."** Two independent wake-source APIs
(ext0, ext1) tried, one real bug found and fixed along the way (missing pull resistors), one
external, credible lead investigated properly (not dismissed) - and the underlying problem turned
out to be a hardware/clock-domain dependency on USB attachment state, which no amount of
GPIO/wakeup-API tuning can work around. Any future attempt needs to start from *that* problem
(why does RTC-GPIO wake apparently need USB-attached clock keepalive to function on this exact
board), not from trying yet another wakeup API - and probably needs a custom Arduino-ESP32 core
build to even investigate properly, not application-level code.

**...update the same evening: step 6 UN-closed. This section is now stale - see cont. 7 below,
which supersedes the "closes step 6 for real" conclusion above with a working approach.** Left the
paragraph above intact rather than deleting it - the reasoning in it (ext0/ext1 both blocked by a
USB-clock dependency) is still correct and worth keeping, it just turned out not to be the whole
story: a GPIO wake source isn't the only way to wake from light sleep.


2026-07-20/21 (cont. 7) — Step 6 retry #3 (timer-poll, no GPIO wake source) - WORKING, user soak-testing
------------------------------------------------------------------------------------------------
User's own idea, prompted by re-examining the cont. 5/6 logs: `esp_light_sleep_start()`'s **timer**
wake had been 100% reliable in *every* test across both prior attempts (`rc=0`, clean, every single
cycle, no exceptions - only the RTC-GPIO wake trigger itself was ever unreliable). So instead of
depending on ext0/ext1 at all, this version doesn't configure a GPIO wake source - it just polls
GPIO0 with a plain `digitalRead()` every time the periodic timer wakes it (200ms interval), and
treats a LOW reading as "wake now". GPIO0 never switches to RTC-IO function this way - it stays in
Button2's own digital mode the whole time - which sidesteps the entire class of problem cont. 5/6
hit, rather than working around it. Tradeoff: button response is bounded by the poll interval
(≤200ms) instead of instant, judged an acceptable cost given two failed attempts at instant wake.

**Confirmed working on real battery on the first genuine test** - the first time any wake mechanism
in this whole step-6 saga has worked off USB power. Then three more real bugs surfaced through
actual use, each found and fixed same-session:

1. **Wake press got misclassified as a long-click, jumping into the face picker instead of just
   waking.** Root cause: the original design deliberately let Button2 see and classify the
   wake-triggering press itself (see cont. 5's original comment, now stale). Button2 hadn't polled
   in seconds (asleep the whole time) - handing it a still-down button as its first fresh reading
   let it measure the hold from an arbitrary starting point. **Fix**: consume the wake press
   entirely inside `deep_idle_loop()` - call `screen_on()` directly the moment a LOW read is seen,
   then busy-wait for the physical release before returning control to the normal loop, so
   Button2's first post-wake poll always sees a clean "not pressed" state and only a genuinely new
   press after this one ever gets classified as a click.

2. **A held-a-bit-longer wake press (past a first-pass 2s release-wait cap) still flashed the
   screen on then immediately off.** The release-wait loop's timeout was too short - a plain
   "hold the button a little longer than a quick tap" (very normal human behaviour, especially
   given the up-to-200ms response lag makes people unsure whether their press registered) exceeded
   2s, handed the still-down button to Button2 after all, which read it as a fresh press on an
   already-awake home screen and fired the existing press-to-turn-off toggle - instantly undoing
   the `screen_on()` from fix #1. **Fix**: extended the release-wait cap to 15s (only meant as a
   safety net against a genuinely stuck/shorted pin, not a real expected wait - any realistic human
   hold is comfortably under that).

3. **With fix #2 in place, the screen wouldn't visibly light up at all until the button was
   released** ("won't start up until I take my finger off the button" - but a snappy tap still
   worked instantly). Root cause: `screen_on()` only sets `screenTimer`'s flag/timestamp - the code
   that actually restores the physical backlight (`screenBrightness()`) lives further down in the
   *same* `hal_loop()` call, which is never reached until `deep_idle_loop()` returns - and fix #2's
   release-wait loop blocks synchronously right there, so nothing calls `screenBrightness()` until
   release. **Fix**: call `screenBrightness(lv_slider_get_value(ui_brightnessSlider))` directly,
   immediately after `screen_on()`, so the physical backlight comes on right away regardless of how
   long the subsequent release-wait blocks for. Needed a forward declaration
   (`void screenBrightness(uint8_t value);` near the other forward decls) since the real definition
   sits much later in the file than `deep_idle_loop()`.

**User confirmed all three fixes working together on-device**: quick tap wakes instantly, a longer
held press now lights the screen immediately too and stays on properly after release, in both
cases resuming to the watch face rather than the face picker.

**Status: NOT YET COMMITTED, deliberately.** User wants to soak-test on real battery for about a
day before finalizing - this note exists so a fresh session picks up the right state rather than
re-deriving it. Current uncommitted diff is entirely in `hal/esp32/app_hal.cpp` (~85 lines): the
`on_battery()` helper, `staying_on_for_charging()` switched to use it, `deep_idle_loop()`
(timer-poll + consume-press + extended release-wait + immediate backlight restore), the
`hal_loop()`/touch-sleep-block call sites switched to `on_battery()`, and the `screenBrightness`
forward declaration. If resuming without the user around: **do not commit or revert this
automatically** - ask first, the soak-test outcome isn't known yet.


2026-07-22 — citizen_410 shadow/LCD polish, flash-method NVS bug found+fixed, step 6 soak
test confirmed (~10x), both PR'd upstream, timeout dropdown +45/60s
------------------------------------------------------------------------------------------------
**citizen_410 visual polish, committed `b46ebcb`.** LCD date/battery panel backgrounds (the
blurred-photo texture behind the day/date/battery digits) made fully transparent
(`bg_opa`/`bg_image_opa` both `LV_OPA_TRANSP`) so the dial/body shows through instead - text
position and the day/date divider nudged to compensate. Date number switched to right-justified
(was center). Pivot cap (the small circle covering the hands' hub) went through a few rounds: user
asked to remove it, then to bring it back "sampled from the second hand's colour" - actually wrote
a small Python script (not checked into the repo, one-off) that decodes the RGB565+alpha pixel data
straight out of `face_citizen_410_hand_second.c`'s generated array, filters for high-alpha
low-green/blue (true red) pixels, and averages them - landed on `0xF41220`, close to the sprite's
purest red cluster. Cap resized 12px -> 8px same pass. Shadow drop-copies got a cheap "blur":
LVGL's own `shadow_width` box-shadow only blurs a widget's rounded-rect, not glyph shapes, so this
fakes it the same way multi-layer CSS text-shadow does - 4 extra copies of each shadow label at
±1px diagonal offsets and low opacity (`create_shadow_halo()` helper, shared by day/date/battery).
Went through two more rounds of "tone down the blur" (`LV_OPA_40` -> `LV_OPA_20`) and "lighten/then
exactly-halfway the shadow colour" (`0x5a5a5a` -> `0x787878` -> `0x696969`, the last one computed as
the literal midpoint of the other two, not eyeballed).

**Flash-method bug found: merged single-file flashing was silently wiping NVS (settings) on every
reflash - not present before this session.** User noticed Extreme Power Save's settings kept
resetting and correctly guessed it was new this session. Root cause: earlier in the session,
flashing used `firmware/lolin_s3_2_06_v6.3_0x0.bin` - the project's own post-build merge script's
single-file output, the same artifact used for the 2026-07-20 GitHub release - written whole to
offset 0x0. That image spans the *entire* flash region in one write, so it blanks `nvs` (0x9000,
confirmed via `partitions_16M.csv`) and `ffat` along with everything else - correct for a factory
flash, wrong for iterative dev use. All prior sessions used `pio run -t upload` (or equivalent),
which only touches `bootloader.bin`@0x0, `partitions.bin`@0x8000, `firmware.bin`@0x10000. **Fixed
by flashing those three files individually at their real offsets instead of the merged image** -
this is now the standing method for this project, not just a one-off; `nvs`/`ffat` are never
touched by a normal dev flash again.

**citizen_410 PR'd upstream: https://github.com/fbiego/esp32-c3-mini/pull/48.** Built on a fresh
branch (`citizen-410-face`) off `upstream/main`, not off `gremlinDev` directly, since `gremlinDev`
has Extreme Power Save (uncommitted at the time) and other in-progress faces
(`classic_410`/`75_2_410`/`red_magic_410`) tangled into the same shared files. Discovered along the
way that the local fork's `main` was 4 commits stale - the AXP2101 battery fix and 1167_410 fix
(PRs #45/#46, opened 2026-07-13) had already been merged upstream, so `app_hal.cpp`,
`platformio.ini`, and `pins.h` needed **zero** changes for this PR; citizen_410 only needed its own
11 files plus one `#define ENABLE_FACE_CITIZEN_410` in `app_hal.h` and two registration lines
(`init_face_citizen_410`/`update_check_citizen_410`) in `ui.c`. Verified the isolated branch
actually builds standalone before pushing, not just "compiles as part of the big branch."

**Extreme Power Save step 6 soak test came back confirmed: ~10x battery life improvement**,
committed as-is (`9121f7c`, same ~85-line diff cont. 7 above described as pending). Before PR'ing
it, walked through what "how contained is this to the Waveshare board" actually means in the code
(not just asserted it): steps 3-6's *behavior* was already fully `#if ESPS3_2_06`-gated, and
`platformio.ini` only defines that macro in one env (`lolin_s3_2_06`) - confirmed zero risk to
other boards. But the Settings switch's *UI* (panel/icon/switch/label creation in `ui.c`, its event
callback registration, and the saved-state restore in `hal_setup()`) was **not** gated the same
way - every other one of the ~20 supported boards would've shown a working-looking toggle that
silently does nothing. Fixed by wrapping those three specific call sites in `#if ESPS3_2_06` too
(`5f2b328`) - deliberately *not* the four `lv_obj_t*` declarations or the two handler function
bodies, which are harmless dead code elsewhere and don't need touching.

**Extreme Power Save PR'd upstream: https://github.com/fbiego/esp32-c3-mini/pull/49**, same
isolated-branch-off-`upstream/main` technique as citizen_410's PR, leading with the 10x result per
user's explicit request. `app_hal.cpp`'s diff against upstream turned out to be 100% Extreme Power
Save with nothing else mixed in (confirmed by grepping hunk headers), so that file came across
wholesale; `ui.c` needed the same kind of surgical hand-reapplication as the citizen_410 PR did,
since it's shared with face-registration lines that don't belong in this PR.

**Screen Timeout dropdown: added 45 and 60 second options**, inserted between "30 Seconds" and
"Always On" (`ui.c`'s `lv_dropdown_set_options` string). The stored preference is the raw dropdown
*index*, not a duration value, and `setTimeout()` was previously computing durations off a `10000 *
i` formula that only happened to work because every option was a multiple of 10 seconds - 45
doesn't fit that pattern, so rewrote it as an explicit `timeoutDurations[]` array with an
"always on if index == array length" sentinel, in **both** `hal/esp32/app_hal.cpp` and
`hal/pico/app_hal.cpp` (duplicated logic, kept in sync - `hal/sdl2` doesn't implement timeout at
all, left alone). **Known caveat, flagged to user, not silently fixed**: inserting the new options
before "Always On" shifts its index from 4 to 6 - anyone with a previously-saved "Always On"
preference (including this dev unit, mid-session) will read back as index 4 ("45 Seconds") until
they revisit Settings and reselect it. User's call was to just re-pick it by hand rather than add a
migration for a two-option dropdown change.

**State as of end of session**: `gremlinDev` has the citizen_410 polish and both Extreme Power Save
commits (`b46ebcb`, `9121f7c`, `5f2b328`) pushed. Timeout dropdown change (`ui.c`,
`hal/esp32/app_hal.cpp`, `hal/pico/app_hal.cpp`) is flashed and confirmed working but **not yet
committed** - straightforward, no open questions, just hasn't been asked for yet. Two upstream PRs
open (#48 citizen_410, #49 Extreme Power Save), neither reviewed/merged yet as of this note.


2026-07-30 — status check-in, both upstream PRs merged
------------------------------------------------------------------------------------------------
Picked the project back up; nothing changed in the repo since the 2026-07-22 session, so this is
just a status correction. **Both PRs merged same day they were opened**: #48 (citizen_410) merged
2026-07-22T15:07:52Z, #49 (Extreme Power Save) merged 2026-07-22T15:09:37Z - confirmed via `gh pr
view`. The "neither reviewed/merged yet" line above is now stale.

Timeout dropdown change committed (`efb260b`), same session - not yet pushed. Also noticed
`tools/facegen/__pycache__/` is untracked and not in `.gitignore` - not touched, flagging for
whenever someone's next in that area.

**citizen_410: merged body_bg + dial_img into one baked asset, user's idea.** The face used two
separate full-screen-ish images every frame: `face_citizen_410_body_bg` (410x494, opaque, the
watch's case/crown/pushers/bracelet) drawn at (0,0), with `face_citizen_410_dial_img` (410x410,
RGB565+alpha, the pattern-matched dial photo) drawn on top at `(DIAL_SHIFT_X, Y_OFFSET)` = (-3,
42), its transparent corners letting body_bg show through in the four triangular gaps. Since
body_bg was already re-cropped specifically to match wherever the dial lands (per its own header
comment), there was no real reason left for two draws instead of one.

Wrote `tools/facegen/merge_citizen_410_bg.py` (checked in, unlike the earlier one-off pivot-cap
colour script) - parses both hand-authored asset `.c` files (they use a plain `_data[]` array name,
not bin2lvgl's `_data_0[]`, so it doesn't reuse `lvgl_img.py`'s existing `parse_asset()` regex),
decodes both to RGBA via `lvgl_img.decode_to_rgba`, alpha-composites dial onto body at the exact
same offset citizen_410.c drew them at, flattens to opaque RGB565 (the merged image always fully
covers the screen so no alpha channel is needed), and writes the new
`face_citizen_410_face_bg.c`. Verified the composite visually (decoded back out to a PNG) before
wiring it in - clean bezel edge, no seam, bracelet lugs land correctly in the corners.

`citizen_410.c`/`.h` updated: single `face_bg` object/image instead of `body_bg`+`dial_img`, one
`lv_image_create`+`lv_image_set_src`+`lv_obj_set_pos(0,0)` instead of two. `DIAL_SHIFT_X`/
`Y_OFFSET` `#define`s kept - still feed `DATE_BOX_X/Y`, `BATT_BOX_X/Y`, `SCREEN_CX/CY` - but a
comment now flags that re-nudging the dial requires re-running the merge script with a new
`DIAL_SHIFT_X`, not just editing the `#define`, since the offset is now baked into the raster.
Old `face_citizen_410_body_bg.c`/`face_citizen_410_dial_img.c` deleted (confirmed no other
references first). `face_citizen_410_dial_img_preview` (the 160x160 face-picker thumbnail) is a
separate asset, untouched.

Confirmed `pio run -e lolin_s3_2_06` builds clean: Flash 78.6% (7413881/9437184 bytes), RAM 45.4%.
Net flash saving vs the two-image version is essentially the old `dial_img` asset's whole size
(~492KB: RGB565+alpha 410x410 = 504300 raw bytes) - `face_bg`'s raw byte count is identical to the
old `body_bg` alone (410x494x2 = 405080 bytes) since only its pixel content changed, not its
dimensions or format. **Update same session: flashed to real hardware (3-segment method), running
stable, user confirmed "That's fine" - committed `f8ae45b`, pushed to `origin/gremlinDev`.** Timeout
dropdown change (previous section) also committed `efb260b` and pushed same session.


2026-07-30 (cont.) — classic_410: ported "cogs" mechanical window from the original RetroPie
prototype, three iterations so far, NOT YET COMMITTED
------------------------------------------------------------------------------------------------
User wanted the spinning-cogs decoration back - this project's `classic_410` face is itself a
port of the user's own AnalogFace app (OLEDS3Watch-joaquim), which in turn traces back to the
very first prototype of this whole watch-face concept: a standalone LVGL-linux simulator running
on a RetroPie Pi at 192.168.0.37 (`/home/pi/lv_port_linux/src/watch_face.c`, LVGL 9.5.0, user
account pi/raspberry). That prototype has a fully-working cogs window that never made it into
either ESP32 port - OLEDS3Watch-joaquim's `app_analogface.cpp` has dead stub constants for it
("Cog window hidden pending banding investigation") but no working implementation. Per user's
explicit instruction (same SSH access pattern previously authorised for a different project),
pulled the real implementation via `sshpass -p raspberry ssh pi@192.168.0.37`, read
`create_gear()`/`create_mech_window()`/`gear_timer_cb()` directly off the Pi (not from the dead
stub), and ported it into `classic_410.c`.

**Iteration 1 - basic port, committed nowhere yet (superseded by iteration 2 same session):**
Ported `create_gear()` near-verbatim (arc-widget rim + radial teeth + hub, each gear a plain
`lv_obj` rotated as a whole via `transform_angle`/`transform_pivot_x/y` - LVGL applies a widget's
transform to its full subtree, so rim+teeth+hub turn together despite being separate child
objects). Added a `lv_timer_create(gear_timer_cb, 50, NULL)` running at the source's own 50ms
cadence, gated with `if (lv_screen_active() != face_classic_410) return;` so it doesn't spend
cycles animating when this face isn't the visible screen (no other face in this codebase uses a
self-ticking `lv_timer` - existing ones are all driven by the shared once-a-second
`update_check_*` cadence from `hal_loop()`, but that's too coarse for smooth gear rotation).
Window: 128x83 rounded-rect, dark fill, red border, positioned at 12 o'clock 0.45*radius up from
centre (same formula as the source's `create_mech_window()`). Numeral "12" skipped from Pass 3's
draw loop (`i==0`) to avoid it, following this file's own existing convention for "3"/date-window
rather than the source's literal "draw all 12, let the opaque window occlude it" approach. Build
clean, flashed via the safe 3-segment method, booted stable. **User confirmed on-device: cogs
spin, hands correctly pass in front near 12, "12" absence not an issue at this stage.**

**Iteration 2 - keystone-shaped window, same session:** User asked for the window's top/bottom
edges to become arcs concentric with the dial and its sides to become straight radial lines
(instead of a rectangle) specifically so the top edge could sit *inside* the "12" numeral's own
radius band and let "12" be drawn again. Radius math: numerals sit at a fixed radius 143 with a
~44px-tall label box, i.e. occupy radius band ~121-165: the *original* rectangular window's flat
top (at radius 128) actually already cut through the bottom of "12" even at dead centre, which is
why iteration 1 had to skip drawing it at all. New constants `COG_OUTER_R=115`,
`COG_INNER_R=32` (same 83px thickness as the old rect height, deliberately unchanged - user said
"don't touch the cogs as tall at this time"), `COG_HALF_ANGLE_DEG=20` (picked so the wedge's top
corners clear the "1"/"11" numerals at ±30°, verified numerically). Frame drawn as a new Pass 4
on `dial_canvas` (filled arc-band via `lv_draw_arc` with `radius`=band midline,`width`=thickness,
plus two border arcs at the outer/inner radii and two straight radial side lines) - kept as its
own canvas pass rather than folded into Pass 1 to respect this file's existing ≤13-draw-tasks-
per-pass rule (ported from OLEDS3Watch's own banding fix, ~~ same LVGL canvas layer-queue-overflow
constraint applies here). `mech_win` (the gear-clipping rect) restyled fully transparent since the
visible frame is now canvas-drawn behind it, and repositioned (not resized) to
`SCREEN_CY - COG_OUTER_R` so it still lines up with the new frame - **gears themselves
(`ga_x/y/r` etc.) left 100% untouched**, only the window around them moved. "12" numeral restored
to Pass 3. Important LVGL gotcha hit here: `lv_draw_arc_dsc_t`'s `start_angle`/`end_angle` use a
**different angle convention** than the clock-angle convention (0deg=12 o'clock, clockwise) used
everywhere else in this file for ticks/hands/numerals - LVGL's arc angles are 0deg=3 o'clock,
clockwise (confirmed by reading `lv_draw_arc.h`'s own doc comment, not assumed) - converted at the
two points of use (`lvgl_angle = 270 + clock_angle`) rather than changing the shared convention.
Build clean, flashed, booted stable. **User confirmed the shape looks right and "12" is back**,
but also flagged (as fully expected/flagged in advance) that the untouched gear cluster - still
sized/laid out for the old 128px-wide rectangle - now visibly overflows the new, narrower-at-the-
bottom wedge shape, most visibly on the left-hand gear's left edge. User explicitly called this
out as "an example of the erasing to be done" for the next step, i.e. confirmed a hard clip
(matching what the rectangle's own edges already do today) is exactly the desired fix, not a
symptom to redesign around.

**Iteration 3 - double the angle, hard-clip gears to the keystone, two colour fixes, same session
(current HEAD state, uncommitted):**
1. `COG_HALF_ANGLE_DEG` 20 -> 40 (user: "double the angle between the 2 edge radii"). Verified
   this still doesn't touch the "1"/"11" numerals - the outer radial excess at the *top* corners
   was already there at 20 deg and is unaffected by widening the angle (it's a radius issue, not
   an angle-span issue); the *bottom* corners' angular excess (already outside the cone at 20 deg,
   still outside at 40 deg) is unaffected either way.
2. **Erase mask ("a window is a window")**: LVGL 9.3 has no built-in way to clip a widget subtree
   to an arbitrary (non-rectangular) shape - the old v8 angle/radius `lv_draw_mask_*` API is gone,
   only `lv_draw_sw_mask_rect` (rectangle only) survives in this version (confirmed by grepping
   the vendored LVGL source directly, not assumed). Worked around it with a same-size ARGB8888
   overlay canvas (`lv_canvas_create` + `LV_COLOR_FORMAT_ARGB8888`, `w*h*4` buffer) positioned
   exactly over `mech_win` and created *after* the gears so it paints on top of them: filled fully
   transparent via `lv_canvas_fill_bg`, then a one-time (not per-frame - the sector shape is
   static, only the gears under it animate) 128x83-pixel loop tests each pixel's radius/angle from
   the dial centre (`atan2f(dx,-dy)` for the clock-angle convention, same conversion note as
   above) and paints opaque black via `lv_canvas_set_px` wherever it falls outside the sector,
   leaving the interior transparent so gear pixels show through untouched. ~10.6k pixel tests,
   trivial one-off cost on an ESP32-S3.
3. **Hub colour black -> white** (user: "the inside of the cogs is black, I think that should be
   white") - `create_gear()`'s hub `bg_color` changed from `lv_color_black()` to
   `lv_color_white()`.
4. **Stray coloured "disc" on each gear's rim -> black** (user: "there is a disc on the outside of
   the cog which should be black"). Root cause confirmed by reading LVGL's own default theme
   source (`lv_theme_default.c`): **every** `lv_arc_create` widget gets an accent-coloured
   circular knob (`theme->styles.knob`, `LV_PART_KNOB`, `bg_color=color_primary`,
   `radius=CIRCLE`) applied automatically unless explicitly overridden - the RetroPie source never
   disabled this (it apparently didn't matter/wasn't noticed there), so every gear's rim arc was
   quietly getting a themed knob blob riding on it. Fixed by explicitly styling
   `LV_PART_KNOB` on the rim arc: `bg_color=black`, `bg_opa=COVER`, `border_width=0`. Not resized
   (default padding/radius left alone) - user only flagged the colour, not the size.

Build clean (Flash 78.6%/7417261 bytes, negligible change from the widget-only code), flashed via
the 3-segment method, booted stable (`Setup done`, no crash-loop; one `bootloader_mmap:
spi_flash_mmap failed: 0x101` line seen once at boot, not obviously related to this display-only
change, not chased). Iteration 3 was confirmed working on hardware and committed (`cca23f8`).


2026-07-30 (cont.) - wake-from-sleep-on-battery regression traced to internal-heap exhaustion,
classic_410 cogs iterated heavily (window narrow/taper false start, teeth taper, colours,
date window), 2151_410 month/day swap
------------------------------------------------------------------------------------------------
**Bug report: watch stopped waking from sleep on battery**, reported right after the cogs commit
above. Confirmed the wake-path code itself (`deep_idle_loop()`/`on_battery()`, step 6) hadn't
changed since its soak-tested, working commit (`9121f7c`) - so not a regression in that code
directly. Root cause found via a live serial boot capture (not just static analysis): **internal
heap at 98.66% used, `E BLE_INIT: Malloc failed` logged at every boot.** `include/lv_conf.h:47`
hardcodes `LV_USE_STDLIB_MALLOC = LV_STDLIB_CLIB`, which overrides the `LV_MEM_SIZE`/`LV_MEM_CUSTOM`
build flags in `platformio.ini` entirely (those only apply under `LV_STDLIB_BUILTIN`) - so
`lv_malloc()` is plain `malloc()` on the same general heap BLE/WiFi need. ESP-IDF's allocator only
spills a single allocation into the 8MB PSRAM once it exceeds a size threshold; anything smaller
always comes from the tiny (~183KB) internal-only heap regardless of how many such small
allocations pile up. classic_410's ~36 gear/tooth/hub `lv_obj_t` widgets (all well under that
threshold) had been landing there permanently since boot, on top of everything else already
enabled - tipping internal heap usage over the edge to where BLE's own re-init (`watch.begin()`,
called from `screen_on()` on every wake) could no longer allocate.

**Fix**: `heap_caps_malloc_extmem_enable(128)` added at the top of `hal_setup()`
(`hal/esp32/app_hal.cpp`, `#if ESPS3_2_06`-gated), lowering the spill threshold so small
allocations also go to PSRAM. Confirmed via two fresh serial boot captures: free internal heap
2504 -> 12256 bytes, `BLE_INIT: Malloc failed` gone both times. **Not yet confirmed by an actual
on-device battery wake test** - the boot-time BLE fix is verified, but nobody has yet pressed the
button on unplugged hardware to confirm wake itself is restored. Ask before assuming it's fully
fixed. A smaller, secondary fix landed first and is still in place: the cogs erase-mask canvas
switched from ARGB8888 (4B/px, ~42.5KB) to A8 (1B/px, ~10.6KB) with `image_recolor` forcing it
black - same visual result, less permanent allocation, though this turned out not to be the real
fix (it's PSRAM either way, LV_MEM_SIZE build flags are dead code per the finding above).

**classic_410 cogs, several more rounds of visual iteration** (all in
`src/faces/classic_410/classic_410.c`, none committed yet):
- Window narrowed: `COG_OUTER_R` 115->110, `COG_INNER_R` 32->37, then bottom (inner) arc raised
  25% more per explicit request: 37->46. `COG_OUTER_R` unchanged at 110.
- Date/day window: moved 3 o'clock -> 6 o'clock (Pass 3's skipped-numeral index changed to match,
  `date_box` repositioned), then moved up in two more passes (radius-from-centre offset 143 ->
  71 -> 40), before landing on 55 - halfway between the 40 and 71 passes, per explicit request
  after "move up a bit more" turned out to be too far. Pass 3's numeral skip was removed
  entirely partway through (date window no longer overlaps any numeral position once it's
  between the centre and "6" rather than sitting on "6" itself), so all 12 numerals draw again.
- Gear hub replaced with two pieces: a `body` ring (same colour as the teeth, sized to reach
  `r - tooth_h` so it touches the teeth with no gap) and a much smaller, fixed-size `spool` at
  the true centre (`22/8=2`, matching the smallest/rightmost gear's own hole size for all three
  gears rather than each scaling its own via `r/8`). Original single white hub was wrong on two
  counts per user feedback: the *whole* centre was white (should only be the middle ring, between
  the true centre and the teeth) and it wasn't noticeably smaller than before.
- Arc rim (`LV_PART_MAIN`) recoloured black instead of the shared metal grey - user described the
  rim as "a circle running around the outside of the cog" that needed to be black. Combined with
  a too-small `body_r` in the same pass, this produced a real visible bug: the (now invisible,
  black-on-black) rim left a visible gap between the small body and the teeth, so the teeth read
  as disconnected floating rectangles ("what am I looking at") - fixed by the `body_r = r -
  tooth_h` sizing above, closing the gap.
- Teeth colour darkened slightly (180,180,180 -> 160,160,160) and the body's fill changed to
  match that same colour instead of white - they'd been visibly mismatched (grey teeth, white
  body).
- **Misstep, reverted**: "the cogs should be narrower on the outside compared to inside" was
  misread as referring to the *window* (`COG_OUTER_R`/`COG_INNER_R`'s own vocabulary happened to
  match), and implemented as a tapered-angle window (interpolated half-angle 40deg at the inner
  edge to 25deg at the outer, applied to the fill via stacked rings, the border, and the erase
  mask). User corrected: that whole sentence was a continuation of the *teeth* discussion (the
  message's first two sentences), not the window - "you've fucked the window right up." Fully
  reverted (window back to the original constant-angle sector, `cog_half_angle_at_r()` helper
  removed). **Lesson: when a request bundles several small tweaks together, a later sentence
  should be read as continuing the immediately-preceding subject unless it clearly changes topic
  - don't let a coincidental name match (this file's own `COG_` constants) override that.**
  Implemented properly on the teeth instead: each tooth split into two stacked pieces (a
  full-width base near the gear body, a narrower (60%) tip at the outer edge) - a 2-step
  approximation of a tapered gear-tooth profile, doubling tooth object count (30->60 total across
  the three gears) but safe now given the PSRAM heap fix above routes these to PSRAM rather than
  the scarce internal heap.

**2151_410** (`src/faces/2151_410/2151_410.c`): month and day digit slots swapped
(`update_time_2151_410()`) - whatever value fed the left digit pair now feeds the right and vice
versa, so the two groups trade on-screen positions. No widget/position changes, just which value
feeds which existing slot.

**State at end of session**: three files modified, nothing committed -
`hal/esp32/app_hal.cpp` (PSRAM heap fix), `src/faces/2151_410/2151_410.c` (month/day swap),
`src/faces/classic_410/classic_410.c` (all the cogs iteration above). All flashed and verified on
the device via `esptool verify-flash` digest checks after each change, but the cogs visual
iteration in particular is still an open loop - last state flashed is the teeth-taper version,
not yet confirmed by the user. `tools/facegen/__pycache__/` still untracked/ungitignored, still
not touched. Mid-session the device's USB connection dropped and reappeared on its own a short
time later (`/dev/ttyACM0` vanished, then came back) - treated as a physical/USB flake, not
chased further, no code implicated.


2026-07-31 - classic_410 cogs rebuilt as raster images (chrome art masked to a
physically-correct meshing silhouette), replacing the widget-tree teeth entirely
------------------------------------------------------------------------------------------------
User's verdict on the teeth-taper widget version (photographed on real hardware): "look like
shit", plus two direct questions that shaped the rest of this session - **why a 2-step
stacked-rectangle taper instead of one simple trapezoid** (answer: `lv_obj_create` widgets can
only fill an optionally-rounded rectangle via styling, there's no polygon/trapezoid primitive, so
the 2-rect stack was a hack around that limit, not a real tapered shape), and **what the cogs
actually cost on the ESP32** (answer, reasoned from config since `LV_USE_PERF_MONITOR` was never
enabled: display is `LV_DISPLAY_RENDER_MODE_PARTIAL` and the mech window is only 128x64px, so the
20Hz `gear_timer_cb` redraw is bounded to that tiny dirty rect regardless of gear complexity -
unlikely to be "overloading" the chip, but the ~60-object teeth-taper version doubled the CPU cost
of that redraw for zero visual gain given the "looks like shit" verdict).

That cost/quality analysis led to a recommendation, accepted by the user: stop layering more
widget hacks on a fundamentally rectangle-limited primitive, and bake each gear as one raster
image instead (fewer objects - 3 rotated image blits total instead of ~69 separate widgets - and
arbitrary shapes/anti-aliasing become trivial). Iterated in stages, each checked visually before
moving on (screenshots sent via SendUserFile, not flashed until the shape itself was right):

1. **Procedural placeholder** (`tools/facegen/gen_cogs_classic_410.py`): flat-grey gears with a
   real single-polygon trapezoid tooth (base + tip radii, tapered angle, no more 2-rect stack),
   annulus-sector "spoke" windows (same arc-top/arc-bottom/straight-radial-sides shape as the
   main keystone window, per explicit request so a future hand-edit of the source PNG has
   somewhere sensible to start from), and a spool hole. User caught two mismatches against the
   already-flashed version: spool radius was 2.75px not the flashed `22/8=2` (integer-truncated),
   and windows were plain circles, not window-shaped - both fixed.

2. **Physically-correct teeth/rotation** ("scale these cogs correctly, with the correct number of
   teeth and turn at the correct rate for interlocking teeth" - explicit user request, placement/
   tangency deliberately left to the user to lock down later). Introduced a shared module
   (`MODULE=4`): teeth count and pitch radius must satisfy `2*r/N == MODULE` for every gear in a
   meshing train. The existing radii (34, 28, 22, chosen back when this was still a purely
   decorative widget tree) happen to divide out to exact integers at module 4 - 17/14/11 teeth,
   no rounding needed. Rotation rate is now derived, not chosen: meshing gears turn at
   `omega * teeth = constant`, alternating direction along the a-b-c chain (b sits between and
   meshes both, so spins opposite to both - already true of the old arbitrary +3/-4/+6 deltas,
   only the magnitudes were wrong). `gear_timer_cb` now accumulates in float degrees (not the
   integer-degree accumulator the old code used) so the non-integer per-tick deltas (e.g.
   51/14 = 3.643 deg) don't compound rounding error - only the final `lv_image_set_rotation` call
   rounds, the running total stays exact.

3. **User's own hi-res chrome art** (`gear_{a,b,c}_hi.jpg`, dropped into
   `assets/cogs_src/` - took two tries to find, first message said they'd added the files but
   nothing had actually landed on this machine yet). AI-generated/rendered gears with a
   chrome/bevel look, but with two problems for this use: a rounded highlight bevel that puffs
   outside the flat tooth silhouette (real teeth can't mesh if their edges are rounded blobs -
   user's own diagnosis, correct), and the artwork's own window/hole shapes/tooth counts, neither
   matching the module-4 geometry above. User's instruction: "remove stuff outside your original
   silhouette" - i.e. use the procedurally-correct shape as a hard mask over the new art, keeping
   only its colour/texture.

   `tools/facegen/mask_cogs_hi.py`: the hi-res art (1024x1024, near-white background) and the
   procedural silhouette weren't pixel-registered (independently authored), so alignment is
   detected rather than assumed - corner-seeded flood fill (4 corners, thresh=20 to absorb JPEG
   noise around white) finds the true background region in the hi-res image; every hi-res gear's
   teeth tips turned out to reach almost exactly to the 1024-canvas edge (outer radius ~504-512px
   out of 512). `render_gear()` gained a `scale` parameter (zooms pitch_r/module/pad/spool
   together, uniformly) so the *exact* procedural silhouette can be re-rendered at whatever pixel
   density matches that detected radius, then pasted (as an alpha channel, not a colour paste) at
   the detected centre onto a same-size transparent canvas holding the hi-res RGB. Cropped to the
   alpha bbox for a compact master, downscaled (LANCZOS) to the original embed resolution
   (82/70/58px, unchanged by any of this - purely geometric, from `pitch_r+module+pad`).

   **Bug caught before it shipped**: the first masked result showed no transparency at the spool
   hole at all. Root cause - `render_gear()`'s spool was drawn as opaque solid black
   (`fill=(0,0,0,255)`), a leftover from the original flat-widget design where "hole" only ever
   meant "paint it the same colour as the black dial behind it". That trick is silently wrong the
   moment the mask is reused over anything that *isn't* a solid black dial (here, arbitrary hi-res
   RGB) - fixed to a real alpha-0 hole (`fill=(0,0,0,0)`), which looks identical on the actual
   black dial and is correct everywhere else too.

   `tools/facegen/bake_cogs_to_c.py`: encodes whatever's currently in `cogs_src/gear_{a,b,c}.png`
   (masked hi-res chrome, or a future hand-edit) to `src/faces/classic_410/assets/cogs_img.c`,
   matching the codebase's existing asset convention exactly (`LV_COLOR_FORMAT_NATIVE_WITH_ALPHA`,
   RGB565 colour plane + planar A8 alpha, `LV_IMG_DECLARE` in the face header) - deliberately a
   separate script from the generator/masker so re-baking a hand-edited PNG never requires
   regenerating or re-masking it.

**`classic_410.c` changes**: `create_gear()` collapsed from ~115 lines building an arc + up to 60
tooth objects + body + spool per gear, down to a single `lv_image_create()` +
`lv_image_set_src()`/`lv_image_set_pivot()` per gear - `r` is still each gear's pitch radius (same
value the silhouette was built from), positioned by its own centre since the baked image is
slightly larger than `2r` (it includes tooth tips + padding). `gear_timer_cb` rewritten per the
module-4 physics above. The mech-window/erase-mask/recentre logic above `create_gear()` is
untouched - it already worked in absolute screen/window coordinates, independent of how each gear
gets drawn.

**Build**: `pio run -e lolin_s3_2_06` succeeds clean. Flash 79.1% (7461949 bytes, up from 78.6%
before this session - the three baked images add a modest amount), RAM 45.4% (148660/327680,
essentially unchanged - these are flash-resident `const` image data, not runtime heap).


2026-07-31 (cont.) - raster cogs flashed: gear spacing was wrong, then a real mesh-phase
puzzle that hardware disagreed with the math on, then the mech window itself resized/fixed
------------------------------------------------------------------------------------------------
**First flash of the raster gears came back badly wrong** - "do you have 3 different gears?".
Decoded the actual bytes baked into `cogs_img.c` (not just the working PNGs) to check: yes, three
correctly distinct 82x82/70x70/58x58 images, so not a data/encoding bug. Root cause was simpler:
the raster images are `2*(r+module+pad)` across (82/70/58px), noticeably bigger than the *widget*
gears they replaced (`2*r` = 68/56/44px) - `ga_x`/`gb_x`/`gc_x` were never updated for that, so at
the old widget-tuned spacing the bigger images overlapped far more than intended (confirmed via a
local PIL simulation of the exact same placement math before touching firmware again - much
cheaper than another flash/photo round trip).

**Exact-tangent mesh placement**: replaced the eyeballed `ga_x/gb_x/gc_x` with real geometry - two
gears mesh exactly when centre-to-centre distance equals the sum of their pitch radii. Chain
topology only (a-b mesh, b-c mesh, a and c don't touch) - three *mutually* meshing external gears
in a closed triangle is a mechanically impossible odd cycle (same parity argument as a triangle not
being 2-colourable), so this was never going to be a triangle regardless. Added `GEAR_GROUP_SCALE`
(uniform shrink of both position and each gear's own `lv_image_set_scale`, since the raster images
plus exact tangency no longer fit the 128x64 mech window uncropped) and `GEAR_MESH_CLEARANCE` (extra
gap added on top of exact tangency, tuned live against user feedback across several flashes: 0 at
narrow angles read "too close", 14 still narrow read "too far", 18 at wider angles read "still a
tooth-length too far", 8 read closer but wanted "a tiny bit tighter", landed on 5).

**The mesh-phase puzzle - math and hardware disagreed, hardware won.** Independent of spacing,
gear_b's own baked tooth phase is arbitrary (whatever the hi-res source art happened to render) and
unrelated to gear_a/c's - two gears meshing needs a *valley*, not a tooth, facing the contact point.
Derived initial rotation offsets per gear from first principles (tooth 0 sits at clock-angle 0 in
the baked art; a valley needs `contact_bearing - halfTooth` rotation) and snapped the b-c arm angle
so `(bc_angle - ba_angle)` lands on an exact multiple of gear_b's own tooth pitch - meaning gear_b's
one fixed tooth pattern should, in principle, face a valley toward *both* neighbours from a single
rotation value. Verified this numerically (not just visually) two different ways - a local
rotate-and-composite simulation, and a direct "nearest-tooth-offset" calculation - both agreed the
derived phase gives the maximum possible valley clearance (not a tooth collision) at all four
contact checks. **User reported the opposite from the real device, twice, independently, with a
specific fix ("rotate the middle cog by half the angle between teeth")** - tried that fix in
simulation first and it mathematically produces exact tooth-on-tooth collision by the same model,
the polar opposite of what should help. Never found the actual discrepancy between the verified
model and the hardware (checked the compiled macro expansion matched the Python model, checked the
object file was newer than the source so it wasn't a stale build, checked the baked art's tooth-0
position empirically against the "clock-angle 0" assumption - all consistent with the model being
right). Applied the user's confirmed-on-hardware fix anyway (`GEAR_B_PHASE_DEG` simplifies to plain
`GEAR_BC_CLOCK_DEG` now, i.e. the raw bearing with no half-tooth subtraction) and left a comment
flagging the unresolved contradiction for whoever's next in this code. **Lesson: trust the person
looking at the physical device over an internally-consistent theoretical model that can't be
directly observed - two independent, specific, repeated reports beat re-deriving the same proof a
third time.**

**Mech window reshaped**, same session, once the gear mesh itself was confirmed good:
- Radial sides: +-40deg -> +-30deg (11 and 1 o'clock exactly), per explicit request.
- Both arcs brought toward each other by 22.5% of the *original* 64px gap (110-46), then - once
  the heavier clipping on the gears themselves was confirmed to look good - pushed back apart again
  by 7.3% of that same original measurement (not the already-narrowed one). Net: outer 110 -> 101,
  inner 46 -> 55.
- **Clipping bug #1, wrong fix first**: "top arc looks a bit messy, thin" - root cause was the gear
  erase-mask (a same-rect overlay painting opaque black outside the sector) using the *exact* same
  radius/angle bounds as the border's own 2px-wide stroke, with no allowance for the stroke's own
  width - eating roughly half the outer arc's visible line. First attempt added an *outward* margin
  to the mask's bounds to protect the border - wrong: confirmed on hardware this let the mask's real
  clip boundary sit *past* the visible border line instead of at/inside it, so gear teeth could poke
  out beyond the red line before finally getting cut off ("clipped on the outside of the window
  defining lines rather inside"). Reverted that margin entirely (mask back to exact bounds) and
  fixed the actual root cause instead: moved the red border (arcs + radial sides) off `dial_canvas`
  (drawn early, so it sat *underneath* the mask in z-order) into its own small `border_canvas`
  (ARGB8888, needs real colour + alpha unlike the black-only mask) created *after* the mask - the
  border is now simply always on top, independent of however tightly the mask clips.
- **Clipping bug #2**: "a straight horizontal clip on the bottom of the middle cog" - `mech_win`'s
  own rectangle was sized to the bare sector height, so gear content that legitimately extends past
  `COG_INNER_R` before the *curved* mask clips it was hitting the rectangle's own flat edge first.
  Added `GEAR_RECT_PAD` (20px) - initially symmetric (both rect edges), which fixed this but created
  a new problem (see next).
- **Clipping bug #3, caused by fixing #2**: "number 11 is being clipped" - the symmetric padding
  pushed the rect's *top* edge (which sits right at `COG_OUTER_R`) outward too, and the "11" numeral
  bounding box (Pass 3) comes within about 1px of that same radius - close enough that even before
  this session's changes it was already razor-thin, and the extra padding pushed the mask's opaque
  fill far enough out to start covering part of "11". Fixed by making the padding bottom-only (only
  `MECH_H` grows; `mech_y`/the rect's top edge is untouched) - the flat-clip fix didn't actually need
  the top edge moved at all, only the bottom.

**User confirmed this state looks good.** Committed and pushed same session (see git log for the
actual commit) - this note plus `hal/esp32/app_hal.cpp` (PSRAM heap fix) and
`src/faces/2151_410/2151_410.c` (month/day swap), both carried over uncommitted from the
2026-07-30 session and mentioned there, landed in the same commit.

------------------------------------------------------------------------------------------------
2026-08-01 - batman_410: new face added, WIP, not yet flashed/tested on hardware
------------------------------------------------------------------------------------------------
New chronograph-style watchface, `src/faces/batman_410/`, following the same rebuild pattern as
citizen_410: delivered as a generic standalone `batman_watchface.c/.h` (runtime SPIFFS PNG
loading, its own create()/update() API) that assumed a different architecture than this project
uses, so it was rebuilt from scratch as compiled-in `lv_img_dsc_t` assets with the real
init_face_*/update_*_*/update_check_* convention. Two deliveries so far, both kept on disk for
reference: the original `batman_watchface_410x494/` (messy geometry, numerals not evenly spaced,
baked-in subdial numbers) and a redelivered "Fully Clean" `batman_v4_410x494/` (numerals sit on a
real circle, both subdials blank, proper tapered/arrowhead hand art) - `batman_410.c` is built
against the v4 art. Dial center and hand pivots were measured programmatically off the source PNG
(thresholding tick-mark pixels, bounding-box/centroid) rather than eyeballed, per the comments in
batman_410.c/.h. Two subdials only (running-seconds sweep + slow hour-style counter) - the 6
o'clock position is fully taken by the bat emblem/numeral so there's no room for a third.

Wired into the build: `ENABLE_FACE_BATMAN_410` in `hal/esp32/app_hal.h`, include + init/update
calls in `src/common/generated_features.h` and `src/ui/ui.c`, same pattern as every other face.
`pio run -e lolin_s3_2_06` compiles clean (RAM 45.4%, Flash 84.2%) but this has **not** been
flashed or eyeballed on the real device yet - purely a compile-time verification, no confirmation
the dial/hands/subdials actually look right or that the measured centers are correct in practice.

Committed as work-in-progress rather than waiting for a hardware pass, so the state is on record
before further changes. Left untracked (redundant with the extracted folders above, not needed to
build): the three raw delivery zips under `src/faces/batman_410/` (`batman_watchface_410x494.zip`,
its "(4)" duplicate, and the nested "(1)" duplicate inside `batman_watchface_410x494/`) - contents
are identical to the already-extracted PNGs/README/`.c`/`.h` sitting next to them.

Next: flash to hardware, check dial/hand alignment and subdial sweep behavior, then decide whether
v4's art needs another pass or is good to confirm.
